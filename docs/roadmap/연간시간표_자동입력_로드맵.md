# 연간 시간표 나이스 자동입력 — 개발 로드맵

상태: 문서화 완료, 구현 대기  
최종 갱신: 2026-08-18  
기준 커밋: c8464a0  
기술 기준: [연간시간표_기술설계.md](../architecture/연간시간표_기술설계.md)  
실기기 기준: [연간시간표_실기기검증.md](../maintenance/연간시간표_실기기검증.md)

이 문서는 작업 순서, 단계별 완료 조건, 결정과 검증 결과를 계속 기록하는 단일 진행판이다. 완료된 기록을 지우지 말고 상태와 후속 결정을 추가한다.

상태 표기:

- ⬜ 시작 전
- 🔄 진행 중
- ✅ 완료 및 검증
- ⚠️ 구현됐지만 검증 또는 보완 필요
- ⛔ 외부 조건 때문에 중단

---

## 1. 최종 사용자 흐름

1. 연간 시간표 PDF/HWP/HWPX를 선택한다.
2. 필요하면 창체 계획 파일을 추가한다.
3. AI 인식 결과를 날짜·교시 표로 검토하고 잘못된 셀을 고친다.
4. 프로그램이 로그인된 나이스의 현재 반에서 과목·교사 목록을 읽는다.
5. 국, 수, 창 같은 원본 표기와 실제 과목·교사를 매핑한다.
6. 같은 과목의 교사가 여러 명이면 기본/요일/교시/날짜 예외를 정한다.
7. 창 셀은 창체 계획 또는 사용자 선택으로 자율·자치/동아리/진로를 확정한다.
8. 전체 연간 미리보기에서 미해결, 기존 값 충돌, 휴업일을 해결한다.
9. 주 단위로 입력하고 셀 값을 검증한다.
10. 초기 버전은 사용자가 저장하고, 안정화 뒤 명시적 동의형 주 단위 자동 저장을 제공한다.
11. 중단되면 현재 값을 다시 읽어 안전하게 이어서 실행한다.

---

## 2. 전체 완료 조건

다음 조건이 모두 충족돼야 기능 완료로 본다.

- 서로 다른 학교/사용자 환경에서 런타임 과목·교사 목록을 사용한다.
- PDF, HWP, HWPX 중 지원 가능한 입력 경로가 같은 중간 모델로 수렴한다.
- 한 글자 별칭과 사전에 없는 과목을 잃지 않는다.
- 복수 교사, 요일·교시별 교사, 특정 날짜 예외를 지원한다.
- 창을 세부 활동으로 임의 변환하지 않는다.
- 창체 전체 계획과 세부 계획의 중복이 한 수업으로 처리된다.
- 미매핑, 중복, 기존 값 충돌, 수업 없는 날이 실행 전에 드러난다.
- 날짜+교시 기준으로 셀을 선택하고 입력 후 값을 다시 확인한다.
- 특수 명령을 과목으로 선택하지 않는다.
- 주 단위 실패 시 저장하지 않고 정확한 원인을 보고한다.
- 재실행과 재개가 이미 일치한 셀을 중복 처리하지 않는다.
- 실제 개인정보가 테스트·문서·진단 자료에 포함되지 않는다.
- 사용자 설명서와 유지보수 문서가 실제 동작과 일치한다.

---

## 3. 단계 현황

| 단계 | 상태 | 목표 | 핵심 산출물 |
|---|---:|---|---|
| T0 | ✅ | 요구사항·실측·안전 기준 고정 | 기술설계, 로드맵, 실기기 절차 |
| T1 | ✅ | Core 모델과 순수 검증 기반 | Timetable 도메인, 단위 테스트 |
| T2 | ⚠️ | 연간 시간표 문서 가져오기 | 시간표 AI 스키마, 검토 가능한 중간 모델 |
| T3 | ⚠️ | 창체 계획 병합·중복 제거 | 창체 파서, 병합기, 충돌 검토 UI |
| T4 | ⚠️ | 나이스 읽기 전용 시간표 드라이버 | 화면 감지, 주차/날짜/셀/메뉴 수집 |
| T5 | ⚠️ | 과목·교사·창체 매핑 UI | 규칙 편집, 프로필, 카탈로그 재검증 |
| T6 | 🔄 | 전체 실행 계획과 미리보기 | 계획 생성기, 충돌 분류, 실행 게이트 |
| T7 | 🔄 | 한 주 안전 입력(미저장) | 셀 입력·재검증·중단·복구 |
| T8 | ⬜ | 주 단위 저장과 연간 재개 | 저장 검증, 체크포인트, 결과 대시보드 |
| T9 | ⬜ | 다환경 회귀·배포 | 실기기 검증, 설명서, 릴리스 |

---

## 4. T0 — 요구사항·설계 기준

상태: ✅

### 완료 작업

- [x] 기존 프로젝트 구조와 재사용 지점 확인
- [x] 연간 시간표 PDF/HWP/HWPX 입력 요구 정리
- [x] 한 글자 과목 표기와 사용자 직접 매핑 요구 정리
- [x] 창체 별도 계획, 세부 활동, 전체/세부 중복 요구 정리
- [x] 복수 교사와 요일·교시별 교사 요구 정리
- [x] 실제 나이스 학급시간표관리 화면·그리드·메뉴 실측
- [x] 동적 UUID, 실제 메뉴 스크롤, 수업 없는 날 동작 확인
- [x] 개인정보 비식별화 기준 확정
- [x] 기술 설계·단계별 로드맵·실기기 검증 문서 작성

### 완료 기준

- 후속 개발자가 사용자와 다시 요구사항 인터뷰를 하지 않아도 T1을 시작할 수 있다.
- 확인된 사실과 아직 실기기 검증이 필요한 가정이 분리되어 있다.
- 코드 변경 없이 문서만 추가되어 있다.

---

## 5. T1 — Core 모델과 순수 검증

상태: ✅ (T1-a 해석 로직 · T1-b 매핑 규칙 완료. 실행 분류 모델은 T6 에서 함께 정의)

### 작업

- [x] src/NeisAutoFill.Core/Timetable/ 폴더 생성
- [x] 날짜+교시 셀 주소 모델
- [x] 원본 수업 토큰과 정규화 결과 모델
- [x] 일반 수업/창체/특수 명령/취소 메뉴 옵션 모델
- [x] 메뉴 문자열 파서
- [x] 한 글자 별칭 정규화기
- [x] 매핑 규칙과 적용 범위 모델
- [x] 날짜·교시 > 요일·교시 > 요일 > 기본값 우선순위 해석기
- [x] 같은 우선순위 중복 규칙을 충돌로 반환
- [x] 카탈로그 안정 키와 정렬 독립 지문
- [x] 실행 분류와 검증 문제 모델
- [x] 개인정보 없는 단위 테스트 작성

### 반드시 포함할 테스트

- 국→국어, 수→수학 등 표준 별칭
- 창, 창체, (창)이 미분류 창체로 유지
- 사전에 없는 학교 과목이 그대로 보존
- 과목(교사(계정)) 파싱
- 과목명에 괄호가 있어도 오른쪽 교사·계정 파싱
- 보강등록(현장체험학습)이 수업으로 오분류되지 않음
- 같은 과목의 두 교사가 서로 다른 옵션으로 남음
- 동일 우선순위 규칙이 둘이면 첫 항목을 고르지 않음
- 메뉴 순서와 DOM UUID 변화가 카탈로그 안정 키에 영향 없음

### 완료 기준

- Core가 Automation/Generator/App에 의존하지 않는다.
- 브라우저 없이 모든 규칙이 테스트된다.
- 미해결과 충돌을 null 또는 첫 후보로 숨기지 않는다.
- dotnet test 전체 통과.

---

## 6. T2 — 연간 시간표 문서 가져오기

상태: ⚠️ (파싱·검토 UI 완료 / 주차 일부 누락 개선 남음)

### 작업

- [x] 기존 PlanFileExtractor 재사용 어댑터
- [ ] 시간표 전용 AI 요청·응답 DTO
- [x] 날짜·교시·원본 토큰·원본 위치·신뢰도 추출
- [ ] AI 응답 JSON 스키마 검증
- [x] 요일/날짜 교차 검증
- [ ] 같은 날짜·교시 중복 검출
- [x] 학년도·학기·학년·반 힌트 검토
- [x] 인식 결과 수정 가능한 표 UI
- [x] 수정 전 원문과 수정 이력 보존
- [ ] 취소·부분 실패·재시도 처리

### 설계 주의

- 평가계획 SubjectPlan 스키마에 시간표를 억지로 넣지 않는다.
- AI가 교사 또는 나이스 메뉴 항목을 생성하지 못하게 한다.
- HWPX 텍스트는 표 구조가 평탄화되므로 원본 위치가 불명확할 수 있다. PDF 원문과 결과 품질을 비교한다.
- 한 파일이 너무 크면 월/주 단위로 나눠 분석하되 전체 중복 검증은 마지막에 다시 수행한다.

### 완료 기준

- 세 형식 중 실제 지원 가능한 경로가 동일한 TimetableSourceLesson 목록을 만든다.
- 잘못된 날짜와 중복 교시는 입력 화면 전에 차단된다.
- 사용자가 AI 결과를 직접 수정하고 확정할 수 있다.

---

## 7. T3 — 창체 계획 병합·중복 제거

상태: ⚠️ (파서·병합·연결 완료, 실제 샘플 검증 / 검토 UI 남음)

### 작업

- [x] 선택적 창체 파일 입력
- [x] 전체 계획과 세부 계획 구역 인식
- [x] 자율·자치/동아리/진로/미분류 타입
- [x] 날짜·범위·교시·활동명 구조화
- [x] 정확 중복 자동 병합
- [x] 느슨한 중복은 사용자 병합 후보로 제안
- [x] 상충하는 타입·교시·시수를 충돌로 표시
- [x] 연간 시간표의 창 셀과 창체 이벤트 연결
- [x] 남은 창 셀을 사용자가 직접 분류
- [x] 병합 뒤 출처 목록 보존

### 완료 기준

- 전체 계획과 세부 계획에 같은 활동이 있어도 한 수업으로 계산된다.
- 자동 병합 이유를 화면에서 확인할 수 있다.
- 미분류 창 또는 창체 충돌이 있으면 실행이 차단된다.

---

## 8. T4 — 나이스 읽기 전용 시간표 드라이버

상태: ⚠️ (읽기 전 구간 실기기 통과 / NeisScreenKind·진단 리포트·2환경 확인 남음)

### 작업

- [x] NeisTarget에 학급시간표관리 추가
- [ ] NeisScreenKind에 시간표 준비 상태 추가
- [x] 어느 화면에서든 학급시간표관리로 이동
- [x] 현재 학년도·학기·학년·반 확인
- [x] grdWeekByClsByTi를 ID 우선, 구조 검증 폴백으로 탐색
- [x] 주차 목록과 실제 날짜 헤더 수집
- [x] CLX 행 데이터에서 날짜·현재 셀 값 읽기
- [x] 우클릭 가능한 대표 빈 셀 탐색
- [x] 열린 메뉴 오버레이 범위 제한
- [x] 수업/교사 옵션과 특수 명령 분리
- [x] 메뉴 스크롤 상태·전체 항목·동적 UUID 무시
- [x] 메뉴를 취소로 안전하게 닫기
- [x] 시간표 전용 비식별 진단 리포트

### 읽기 전용 원칙

- 이 단계에서는 저장 버튼을 누르지 않는다.
- 메뉴 항목 선택 대신 취소만 사용한다.
- 진단 결과에 실제 교사명·계정 ID를 원문으로 쓰지 않는다.

### 완료 기준

- 현재 반의 날짜, 교시, 셀 값, 전체 할당 가능 옵션을 읽는다.
- 수업 없는 날을 정상적인 CellUnavailable 상태로 구분한다.
- 메뉴 순서와 항목 수를 상수로 사용하지 않는다.
- 두 번째 환경의 카탈로그가 달라도 코드 변경 없이 읽는다.

---

## 9. T5 — 과목·교사·창체 매핑 UI

상태: ⚠️ (창·규칙·프로필 동작 확인 / 특정 날짜 예외 UI·메인 화면 연결 남음)

### 작업

- [x] 원본 토큰별 나이스 후보 표시
- [x] 정확/정규화/유사 추천 구분
- [x] 일반 과목과 창체 후보 구분
- [x] 같은 과목의 복수 교사 표시
- [x] 전체 기본 매핑
- [x] 요일 매핑
- [x] 요일+교시 매핑
- [ ] 특정 날짜+교시 예외
- [x] 입력 안 함과 미해결을 별도 상태로 표시
- [ ] 규칙 우선순위 미리보기
- [x] 중복 규칙 차단
- [x] 범위가 지정된 매핑 프로필 저장
- [x] 카탈로그 지문 변경 시 재확인

### UX 기준

- 기존 MatchPreview UX를 참고하되 일대다 규칙 편집에 맞게 새 화면을 만든다.
- 교사 계정은 필요한 만큼만 마스킹해 같은 이름의 교사를 구별한다.
- “추천됨”과 “사용자 확정”을 시각적으로 구분한다.
- 미선택 값을 같은 이름으로 자동 폴백하지 않는다.

### 완료 기준

- 월요일 창은 담임, 수요일 창은 다른 담당 교사처럼 서로 다른 규칙을 만들 수 있다.
- 한 교사가 목록에서 사라지면 저장된 규칙을 자동 실행하지 않는다.
- 모든 목표 셀의 최종 선택 이유를 설명할 수 있다.

---

## 10. T6 — 전체 실행 계획과 미리보기

상태: 🔄 (계획 생성기·분류·게이트 완료 / 미리보기 UI 는 T5 이후)

### 작업

- [x] 원본 수업+창체+매핑 규칙을 TimetableAssignment로 결합
- [x] 나이스의 현재 값과 비교
- [x] 이미 일치/입력 예정/충돌/미해결/수업 없음 분류
- [x] 날짜·주차별 집계
- [ ] 과목·교사별 집계
- [ ] 기준 시수와 계획 시수 차이 경고
- [ ] 충돌 셀별 사용자 결정
- [ ] 제외 셀 명시
- [x] 실행 가능성 게이트
- [ ] 실행 계획 내보내기(개인정보 주의)

### 완료 기준

- 실제 클릭 전에 연간 입력 결과를 셀 단위로 볼 수 있다.
- 기존 값 덮어쓰기 대상이 숨겨지지 않는다.
- 막힌 항목이 있으면 실행 버튼이 비활성화되고 이유가 표시된다.
- 같은 계획을 다시 만들면 결정적이고 동일한 결과가 나온다.

---

## 11. T7 — 한 주 안전 입력(미저장)

상태: 🔄 (단일 셀 W1~W5 실기기 통과 / 한 주 전체·취소·세션만료 남음)

### 작업 순서

1. 단일 빈 셀 + 미저장
2. 같은 셀 재실행 시 이미 일치 판정
3. 메뉴 스크롤 밖 옵션
4. 같은 과목의 다른 교사
5. 창체 3종
6. 수업 없는 날
7. 기존 값 충돌
8. 한 주 전체 + 미저장
9. 중간 취소와 세션 만료

### 구현 작업

- [x] 실제 날짜가 포함된 주차 선택
- [x] 날짜 열과 교시 행 재검증
- [x] 우클릭 후 대상 메뉴 안정 키 재검색
- [x] 메뉴 자체 스크롤 선택
- [x] 입력 후 셀 재검증
- [ ] 실패 시 한 번만 재시도
- [ ] 한 건 실패 시 해당 주 중단
- [ ] 미저장 변경 상태와 복구 안내
- [ ] 사용자 취소 즉시 반영

### 완료 기준

- 저장하지 않은 한 주 입력이 모든 대상 셀에서 재검증된다.
- 수업 없는 날이나 옵션 실종 시 잘못된 다른 항목을 선택하지 않는다.
- 취소 후 어느 셀까지 변경됐는지 정확히 보고한다.

---

## 12. T8 — 주 단위 저장·연간 배치·재개

상태: ⬜

### 작업

- [ ] 자동 저장 명시적 동의 UI
- [ ] 허용된 저장 확인 대화상자 타입
- [ ] 모든 셀 검증 성공 때만 저장
- [ ] 저장 후 주차 재조회 검증
- [ ] 주 단위 체크포인트
- [ ] 앱 재시작 후 현재 값 재검증 기반 재개
- [ ] 이미 일치한 셀 건너뛰기
- [ ] 실패 주에서 중단하고 다음 주 자동 진행 금지
- [ ] 결과 대시보드와 실패만 재시도
- [ ] 체크포인트와 원본/카탈로그 변경 감지

### 완료 기준

- 한 주가 완전히 검증된 경우에만 완료 체크포인트가 생긴다.
- 저장 후 값이 다르면 즉시 중단하고 완료로 기록하지 않는다.
- 재개 시 과거 기록이 아니라 현재 나이스 값을 최종 권위로 사용한다.

---

## 13. T9 — 다환경 회귀·배포

상태: ⬜

### 검증 환경

- [ ] 과목·교사 구성이 다른 두 개 이상의 계정/학교
- [ ] 같은 과목에 교사가 한 명인 환경
- [ ] 같은 과목에 교사가 여러 명인 환경
- [ ] 창체 세 종류와 서로 다른 담당 교사
- [ ] 휴업일·행사일이 포함된 학기
- [ ] 메뉴 항목 수와 순서가 다른 환경
- [ ] PDF/HWP/HWPX 샘플
- [ ] 창체 전체+세부 계획 중복 샘플

### 배포 작업

- [ ] 전체 dotnet build/test
- [ ] 실기기 검증 기록 승인
- [ ] 개인정보 포함 파일 탐색
- [ ] 사용설명서 업데이트
- [ ] docs/architecture/개발문서.md 파일 역할 업데이트
- [ ] 변경 로그와 버전 갱신
- [ ] 롤백 방법 확인

### 완료 기준

- 전체 완료 조건을 체크리스트로 검증한다.
- 알려진 제한과 수동 처리 방법이 사용자 설명서에 있다.
- 나이스 개편 때 수정할 진단·셀렉터·파서 위치가 문서화돼 있다.

---

## 14. 리스크 목록

| ID | 위험 | 대응 | 상태 |
|---|---|---|---|
| R-001 | 문서 양식이 학교마다 다름 | AI 제안+결정론적 검증+수정 UI | 열림 |
| R-002 | 창체 전체/세부 계획 이중 계산 | 출처 병합, 불명확하면 사용자 검토 | 열림 |
| R-003 | 교사 목록과 순서가 환경마다 다름 | 런타임 카탈로그와 범위 지문 | 열림 |
| R-004 | 메뉴 UUID가 매번 바뀜 | 텍스트 구조+팝업 범위+안정 키 | 대응 확정 |
| R-005 | 수업 없는 날은 메뉴가 안 열림 | CellUnavailable 분류, 날짜 사전 검증 | 대응 확정 |
| R-006 | 화면 표시와 저장 후 값 표현이 다름 | 저장 전/후 별도 읽기 어댑터 실측 | 열림 |
| R-007 | 학기 중 나이스 화면 개편 | 시간표 전용 진단과 실기기 절차 | 열림 |
| R-008 | 부분 입력 후 세션 종료 | 주 단위 미저장 중단, 현재 값 기반 재개 | 열림 |
| R-009 | 기존 시간표 손상 | 기본 덮어쓰기 금지, 충돌 명시 승인 | 대응 확정 |
| R-010 | 진단 자료 개인정보 유출 | 비식별화·커밋 전 탐색 | 대응 확정 |

---

## 15. 작업 기록 규칙

각 단계 작업이 끝날 때 이 문서의 상태·체크리스트와 아래 진행 기록을 같은 변경에서 갱신한다.

기록에 반드시 포함:

- 목표와 범위
- 적용한 결정 ID
- 변경 파일
- 커밋 해시 또는 커밋 없음
- 단위 테스트/빌드 결과
- 실기기 검증 범위
- 새로 확인된 사실
- 실패·제약·남은 위험
- 다음 작업 한 개
- 되돌리는 방법

완료된 사실을 삭제하거나 현재 상태만 덮어쓰지 않는다. 설계가 바뀌면 기존 결정이 왜 대체됐는지 남긴다.

---

## 16. 진행 기록

### 2026-08-19 · T2 인식 결과 검토 UI · ✅

- 목표: 문서에서 읽은 시간표를 사용자가 눈으로 보고 고치는 화면
- 변경 파일:
  - src/NeisAutoFill.App/ViewModels/TimetableReviewViewModel.cs (신규)
  - src/NeisAutoFill.App/TimetableReviewWindow.xaml(.cs) (신규)
- 단위 테스트: 367/367 통과 · 빌드 경고 0
- 화면 검증: 실제 두 문서(연간 시간표.pdf + 창체.pdf)를 넣어 렌더링 확인
  - 주차 15개 목록, 각 주차에 칸 수와 "확인 n" 표시
  - 첫 주(03-03)는 **월요일 열이 비어 있다** — 개학이 화요일이라 정확하다
  - 03-27(목) 7교시 '봉'에 주황 점 — 나이스에 없는 항목이라 확인 대상으로 표시됨
  - 문제 목록에 학년도 불일치·창체 기간 밖·휴업일 경고가 모두 모임
  - 칸 수정 후 EditedCount 반영, BuildLessons 가 수정본을 돌려줌
- 설계 판단:
  - 392칸을 한 화면에 펼치지 않고 **주차 선택 → 그 주만 편집**. 전부 보여주면 못 쓴다.
  - 칸을 비우면 그 시간은 입력 대상에서 빠진다 — 삭제를 위한 별도 조작이 필요 없다.
  - 확인이 필요한 칸(창체 미해결 등)은 주황 점으로 표시하고, [확인할 주로] 버튼으로 바로 이동한다.
- 실패·제약: 되돌리기(Undo) 없음, 원문 대비 수정 이력은 Edited 플래그 수준
- 다음 작업: 문서 → 검토 → 매핑 → 계획을 잇는 진입 흐름, 또는 W6(한 주 전체 입력)
- 되돌리는 방법: 위 신규 3파일 삭제

---

### 2026-08-19 · T3 창체 파서 + 병합 연결 · ✅

- 목표: 창체 계획 문서를 읽어 시간표의 창체 칸과 잇는다
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/CreativeDocumentParser.cs (신규)
  - src/NeisAutoFill.Core/Timetable/CreativeActivityLinker.cs (신규 — 연결 + 문서 짝 검증)
  - tests/NeisAutoFill.Tests/CreativeActivityLinkerTests.cs (14개)
- 단위 테스트: 367/367 통과
- 실제 샘플 검증 (docs/schedule/창체.pdf):
  - 일정 **52건** 인식 — 자율 43 · 동아리 6 · 진로 2 · 미분류 1 (원본 구성과 일치)
  - 병합 **52 → 46건**: 정확 중복 3쌍(03-16 인성교육주간, 05-11 흡연예방, 05-15 보건)이 한 건으로
  - 충돌 1건: 03-26 "1학기현장체험(5)" 이 자율 2건 + 진로 1건 → 임의 선택 없이 사용자에게 올림
  - 연결: 시간표 창체 칸 **37개**(자27 동6 진2 봉2) 중 **35칸 자동 확정**(FromSource), 봉 2칸만 미해결
- 확인된 사실:
  - 창체 종류는 표 제목이 아니라 **행마다 적힌 '영역' 칸**에서 읽는 것이 정확하다.
  - 열 경계는 헤더 라벨 사이 중점으로 잡되, **학습주제 칸만은 라벨보다 왼쪽에서 데이터가 시작**한다
    ("교육활동…" → "육활동…" 잘림) → 요일 칸 바로 오른쪽부터로 잡는다.
  - 주차 번호가 날짜에 붙어 "13.3"(13월)이 된다 → '주' 라벨로 왼쪽 경계를 막는다.
  - 창체 계획에는 **교시가 없다**. 그래서 같은 날 종류가 둘 이상이면 어느 칸인지 알 수 없어 충돌로 둔다.
  - 샘플 두 문서의 **학년도가 서로 다르다**(시간표 2025 / 창체 2026). 파이프라인이 이를 스스로 드러냈고
    `CheckPair` 로 코드에 넣었다 — 다른 학기 파일을 넣으면 활동명이 하나도 안 붙는다.
- 실패·제약:
  - 창체 52건 중 1건은 활동명 앞글자가 잘린다("학급 다모임" → "급모임")
  - 검토·수정 UI 없음
- 다음 작업: 인식 결과 검토 UI, 또는 시간표 주차 누락(15/21주) 개선
- 되돌리는 방법: 위 신규 2파일과 테스트 삭제

---

### 2026-08-19 · T2 시간표 문서 로컬 파싱 (AI 없이) · ✅

- 목표: 연간 시간표 문서를 GAS/AI 없이 로컬에서 날짜+교시 목록으로 변환
- 적용/변경한 결정: **D-014 신규** (아래)
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/DocumentLayout.cs (글자+좌표 모델, Core 는 PDF 라이브러리를 모른다)
  - src/NeisAutoFill.Core/Timetable/TimetableDocumentParser.cs (표 → 수업 목록)
  - src/NeisAutoFill.Core/Timetable/TimetableTokenNormalizer.cs (창체 자·동·진·봉 추가)
  - src/NeisAutoFill.Core/Timetable/TimetableMappingSuggester.cs (창체 종류가 적혀 있으면 후보를 좁힘)
  - src/NeisAutoFill.Generator/PdfLayoutExtractor.cs (PdfPig, MIT)
  - src/NeisAutoFill.Generator/PlanFileExtractor.cs (hwpx 열기 포맷 수정)
  - tests/NeisAutoFill.Tests/TimetableDocumentParserTests.cs (8개)
- 단위 테스트: 353/353 통과
- 실제 샘플 검증 (docs/schedule): **PDF·HWP·HWPX 세 형식이 완전히 동일한 결과**
  `2025학년도 1학기 · 수업 392칸 · 주 15개 · 경고 13`
  토큰 국83 수53 체36 음34 사34 과33 영31 자27 실22 미22 도7 동6 봉2 진2
- 새 결정 **D-014**: 연간 시간표는 <b>AI 대신 좌표 기반 로컬 파싱</b>을 기본 경로로 한다.
  이유: ① 문서가 규칙적인 표라 결정론적으로 읽힌다 ② GAS 재배포·네트워크·비용이 없다
  ③ 같은 입력이면 항상 같은 결과다. 다른 양식이 나오면 그때 AI 경로를 보조로 붙인다.
- 확인된 사실 (전부 실제 문서가 알려준 것):
  - **텍스트 평탄화로는 요일이 하루씩 밀린다.** 학기 첫 주가 화요일 시작인데 hwpx 텍스트에서는
    첫 그룹이 월요일처럼 보였다. 좌표로는 정확히 화요일에 들어갔다 → 좌표 파싱을 택한 결정적 이유.
  - 한글과 숫자의 **기준선이 달라** 표 한 행이 여러 줄로 갈린다 → 줄이 아니라 '기간이 적힌 Y 기준 행 범위'로 모은다.
  - 줄 묶기를 반올림으로 하면 경계값이 갈린다 → 간격 기반 클러스터링.
  - 비고 칸 글자가 마지막 교시 칸으로 샌다 → 헤더의 '비 고' 위치로 오른쪽 경계를 막는다.
  - 한 행 안에서도 수업 토큰은 **한 기준선**에 모인다 → 글자가 가장 많은 기준선만 남긴다.
  - 원본은 창체를 **자·동·진·봉으로 구분**해 적는다. '봉'(봉사활동)은 나이스 메뉴에 없어 미분류로 둔다.
  - 하루 교시 수와 마지막 교시 번호를 고정하지 않는다(6~8교시 학교가 다 있다).
  - `HwpToPdf` 가 포맷을 "HWP" 로 고정해 .hwpx 를 못 열었다 → 확장자에 맞춰 넘긴다.
- 실패·제약:
  - 주차 15개만 잡힌다(1학기는 21주쯤) — 일부 주차 행을 놓치는 것으로 보인다. 다음 개선 대상
  - 경고 13건은 휴업일 표기가 칸을 가로지르는 경우로, 수업으로 오인하지는 않는다
  - 창체 계획 파서는 아직 없다(표 구조는 파악 완료)
  - 인식 결과 검토·수정 UI 없음
- 다음 작업: 창체 계획 파서 → 병합 연결 → 검토 UI
- 되돌리는 방법: 위 신규 3파일 삭제, PdfPig 참조 제거, 토큰 사전 되돌림

---

### 2026-08-19 · T7 단일 셀 입력 W1~W5 · ✅

- 목표: 셀 입력기를 만들고 실기기에서 단일 셀 시험을 승급 (사용자 승인 아래, **저장 없음**)
- 적용한 결정: D-003, D-006, D-010, R-006 대응
- 변경 파일:
  - src/NeisAutoFill.Automation/TimetableCellWriter.cs (신규)
  - src/NeisAutoFill.Automation/TimetableReader.cs (ReadCellDateAsync, ReadCatalogAsync closeAfter)
  - src/NeisAutoFill.Core/Timetable/TimetableOption.cs (LooseKey)
  - src/NeisAutoFill.Core/Timetable/TimetableCatalog.cs (FindLoose)
  - src/NeisAutoFill.Core/Timetable/TimetableGridParser.cs (열 인덱스 도입, 요일 가정 제거)
- 단위 테스트: 345/345 통과
- 실기기 검증: W1·W2·W3·W4·W5 + 덮어쓰기 차단 전부 통과 (상세는 실기기검증 §4-B)
- 확인된 사실 (둘 다 오판을 만든 실제 결함):
  1. **저장 전 셀에는 계정이 빠진다** (`국어(진두성)`). 정확 키만 비교하면 정상 입력을 실패로 본다.
     → LooseKey 로 보조 판정하되 카탈로그에서 유일할 때만 인정.
  2. **mon/tue/… 는 요일이 아니라 열 순서다.** 주차가 수요일에 시작하면 monYmd 가 수요일이다.
     요일로 계산하면 엉뚱한 날에 입력한다 → 날짜는 Ymd 에서만, 열은 ColumnOf 로.
- 실패·제약:
  - 미저장 변경 5칸(08-25 4~8교시)이 화면에 남아 있다 — 조회 시 '아니오'로 버리면 된다
  - W6(한 주 전체)·B2(중간 취소)·B3(세션 만료) 미실시
  - **저장(S1) 미실시** — 별도 승인 필요
- 다음 작업: 시간표 문서 파서(T2 로컬 경로) — 샘플 docs/schedule 확보됨
- 되돌리는 방법: TimetableCellWriter.cs 삭제, LooseKey/FindLoose/Columns 되돌림

---

### 2026-08-19 · T5 세션·프로필 연결 + 개인정보 수정 · ✅

- 목표: 화면 이동~카탈로그~범위~프로필을 하나로 묶고 메인 화면에 진입점 추가
- 범위에서 제외: 셀 입력·저장(T7), 특정 날짜 예외 UI
- 적용한 결정: D-004, D-012, D-013(신규 — 아래)
- 변경 파일:
  - src/NeisAutoFill.Automation/TimetableScopeReader.cs (신규 — 조회 조건·학교·사용자)
  - src/NeisAutoFill.Automation/TimetableTools.cs (신규 — App 이 IPage 를 만지지 않게 하는 묶음)
  - src/NeisAutoFill.Automation/Abstractions/INeisEngine.cs · NeisEngine.cs (Timetable 노출)
  - src/NeisAutoFill.App/Services/TimetableSession.cs (신규 — 이동→조회→주차→카탈로그→범위→매핑)
  - src/NeisAutoFill.App/Services/TimetableProfileStore.cs (저장 스키마를 DTO 로 분리)
  - src/NeisAutoFill.App/ViewModels/MainViewModel.cs · MainWindow.xaml (숨김 [🗓 시간표 확인])
  - src/NeisAutoFill.Core/Timetable/TimetableOption.cs (안정 키의 교사·계정 해시화)
  - tests/NeisAutoFill.Tests/BatchUploadRunnerTests.cs (FakeEngine 에 Timetable 추가)
- 단위 테스트: 343/343 통과
- 빌드: 경고 0 · 오류 0
- 실기기 검증 (읽기 전용):
  - 프리플라이트 Ok — 범위 자동 인식 `2026학년도 2학기 5학년 1반 · 1주차`
  - 셀 56칸(값 6칸) · 입력 가능 20 · 제외 명령 9
  - 프로필 저장 → **새 인스턴스로 재로드** 2건 → 재사용 가능 True, 다른 지문이면 재확인 True
  - 실행 계획 CellUnavailable 2 · AlreadyMatches 1 · Pending 2 · 막힘 0
- 확인된 사실 / 잡은 결함 2건:
  1. **`MappingScope` 는 JSON 역직렬화가 불가능했다** (잘못된 조합을 막으려 생성자를 감춴 탓).
     저장은 되고 **불러오기에서 예외** → DI 생성자에서 터져 앱이 시작조차 못 할 수 있었다.
     저장 스키마를 DTO 로 분리하고, 로드 실패는 어떤 예외든 빈 목록으로 흡수하게 고쳤다.
  2. **안정 키에 교사명·계정 원문이 들어가 매핑 파일에 남았다**(`L|과학|이문성|#p1000…`).
     사람 정보만 해시로 바꿨다(`L|과학|9fdcd68f|be43edc5`). 과목명은 개인정보가 아니라 유지.
- 새 결정 **D-013**: 안정 키의 사람 정보(교사명·계정)는 해시로만 만든다. 저장 파일·진단 리포트에
  원문이 새지 않게 하기 위함이며, 카탈로그와 규칙이 같은 방식으로 키를 만들어 대조는 유지된다.
  (기존에 저장된 프로필은 지문이 달라져 무효화되지만, 이 기능은 아직 릴리스 전이라 영향 없음)
- 실패·제약:
  - 진입점은 **숨김 버튼**(❓ Ctrl+Shift+클릭으로 노출)이다 — 원본 시간표(T2)가 없어 아직 입력까지 못 간다
  - 특정 날짜 예외 UI 미구현
- 다음 작업: T2(GAS 재배포 필요) 또는 T7 준비. T7 은 나이스 쓰기라 사용자 승인 후 시작
- 되돌리는 방법: 위 신규 4파일 삭제 + INeisEngine/NeisEngine/MainViewModel/MainWindow 변경 되돌림

---

### 2026-08-18 · T5 매핑 화면 · ⚠️

- 목표: 원본 표기를 나이스 과목·교사와 잇는 화면과 규칙 생성
- 범위에서 제외: 특정 날짜 예외 편집 UI, 메인 화면에서 창 열기(다음 단계)
- 적용한 결정: D-002, D-006, D-007, D-008, D-012
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/TimetableMappingSuggester.cs (제안·자동 확정 경계)
  - src/NeisAutoFill.Core/Timetable/TimetableMappingProfile.cs (범위·재사용 판단)
  - src/NeisAutoFill.App/Services/TimetableProfileStore.cs (timetable_mappings.json)
  - src/NeisAutoFill.App/ViewModels/TimetableMappingViewModel.cs
  - src/NeisAutoFill.App/TimetableMappingWindow.xaml(.cs)
  - src/NeisAutoFill.Core/Timetable/TimetableMappingRule.cs (Describe() → Description 프로퍼티)
- 단위 테스트: 343/343 통과 (325 → 343, +18)
- 빌드: 경고 0 · 오류 0
- 화면 검증: 임시 WPF 하네스로 창을 띄워 자체 렌더링 캡처. 실기기 카탈로그(29항목) 그대로 투입
  - 초기: 표기 5종 · 미해결 4 · **적용 불가** (국어만 후보 1개라 자동 추천)
  - 선택 후: 확정 4 · 입력 안 함 1 · 미해결 0 · **적용 가능**
  - 요일 규칙: 창이 쓰인 월·수에 각각 규칙 생성 → 목록에 "창 → 월요일", "창 → 수요일"
- 확인된 사실:
  - **WPF `Run` 의 중첩 경로 바인딩(`{Binding Scope.Description}`)이 표시되지 않는다.**
    같은 DataContext의 단일 프로퍼티 Run 은 보이는데 중첩 경로만 비었다.
    TextBlock 을 나란히 두는 방식으로 해결했다. 앞으로 인라인 Run 에 중첩 경로를 쓰지 않는다.
  - `Describe()` 를 프로퍼티 `Description` 으로 바꿨다 — 메서드는 XAML 바인딩에서 쓸 수 없다.
- 실패·제약:
  - 특정 날짜 예외(SpecificDate)는 모델·해석기에는 있으나 화면에서 만들 수 없다
  - 매핑 프로필을 실제로 저장·복원하는 흐름(범위 수집)은 아직 화면에 연결되지 않았다
  - 메인 화면에 진입점이 없다 — 현재는 코드 호출로만 열 수 있다
- 남은 위험: R-003 완화 유지
- 다음 작업: 메인 화면 진입점 + 프로필 저장·복원 연결
- 되돌리는 방법: 위 파일 6개 삭제, Description → Describe() 되돌림

---

### 2026-08-18 · T4 비식별 진단 리포트 · ✅

- 목표: 나이스 개편 시 근거가 될 시간표 전용 진단을 만들고 실기기로 확인
- 적용한 결정: D-012 (개인정보 비식별)
- 변경 파일: src/NeisAutoFill.Automation/TimetableDiagnostics.cs (신규)
- 빌드: 경고 0 · 오류 0 · 테스트 325/325 통과
- 실기기 검증: 리포트 생성 성공. 교사명 → 교사A~G, 계정 → account-a~g 로 치환 확인
- 확인된 사실:
  - **`grdWeekByClsByTi` 에 `{day}Otpt`·`{day}Info` 필드가 실제로 존재한다.**
    단, **값이 있는 주에서만** 나타난다 — 빈 주에서는 `pir`·`{day}Ymd` 만 온다.
    필드 부재를 실패로 보면 안 되고 빈 셀로 취급해야 한다(파서는 이미 그렇게 동작).
  - CLX 행 필드 목록은 `toObject` → `getRowData` → JSON 복제 순서로 폴백해야 채워진다.
- 실패·제약: 앱 UI(진단 버튼)에 아직 연결하지 않았다 — 현재는 코드 호출로만 실행 가능
- 다음 작업: T5 매핑 UI (또는 사용자 GAS 재배포 후 T2)
- 되돌리는 방법: TimetableDiagnostics.cs 삭제

---

### 2026-08-18 · T4 화면 이동 + 이동~계획 전 구간 통과 · ✅

- 목표: 어느 화면에서든 학급시간표관리로 이동하고, 조회·주차 선택까지 이어 읽기를 완성
- 범위에서 제외: 쓰기·저장 (T7·T8). 이번에도 셀 선택·저장 없음
- 적용한 결정: D-003
- 변경 파일:
  - src/NeisAutoFill.Automation/Abstractions/INeisEngine.cs (NeisTarget.ClassTimetable 추가)
  - src/NeisAutoFill.Automation/NeisEngine.cs (2단계 메뉴 분기 — 시간표는 [시간표관리])
  - src/NeisAutoFill.Automation/TimetableReader.cs (화면 인식/그리드 유무 분리, SelectWeekForDateAsync)
- 단위 테스트: 325/325 통과 (변화 없음 — 이 단계는 실기기 검증이 본체)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: **이동 → 조회 → 주차 선택 → 읽기 → 카탈로그 → 계획 전 구간 통과**
  (상세·수치는 [실기기검증 §4-A](../maintenance/연간시간표_실기기검증.md))
- 확인된 사실:
  - **화면 제목은 "학급시간표 관리"(띄어쓰기), 메뉴 라벨은 "학급시간표관리"(붙임).**
    도착 판정을 붙임 표기로 하면 이동에 성공하고도 실패로 보고한다 — 실제로 이 버그를 냈다가 고쳤다.
  - **조회만으로는 시간표 그리드가 없다.** 주차 목록만 채워지고, 주차를 선택해야 그리드가 생긴다.
    화면 도착 판정에 그리드를 요구하면 안 된다(서술문 화면과 같은 함정).
  - 2단계 메뉴가 평가·서술문([학생평가])과 다르다([시간표관리]).
- 회귀 확인: [교과별 평가]·[학기말종합의견] 이동 정상, 같은 화면 재이동은 740ms 즉시 true
- 실패·제약:
  - `NeisScreenKind` 에 시간표 준비 상태는 아직 추가하지 않았다(상태칩 영향 검토 필요)
  - 시간표 전용 비식별 진단 리포트 미구현
  - 두 번째 환경(다른 학교·계정) 확인 미실시
- 남은 위험: R-003·R-007 열림
- 다음 작업: T5 매핑 UI 또는 T2(GAS). T2 는 여전히 사용자 재배포가 필요하다
- 되돌리는 방법: NeisTarget 항목과 NeisEngine 2단계 분기 제거, TimetableReader.cs 삭제

---

### 2026-08-18 · T4 TimetableReader + 전 구간 실기기 검증 · ✅

- 목표: 진단으로 확인한 방법을 Automation 계층 드라이버로 정식화하고, 읽기→계획까지 실제 화면으로 검증
- 범위에서 제외: 셀 입력·저장 (T7·T8). 이번에도 쓰기는 전혀 하지 않음
- 적용한 결정: D-001, D-003, D-004, D-005
- 변경 파일:
  - src/NeisAutoFill.Automation/TimetableReader.cs (신규 — 주차·셀·카탈로그 읽기, 메뉴는 취소로만 닫음)
  - src/NeisAutoFill.Automation/NeisSelectors.cs (Timings 에 TimetableMenuOpen·TimetableWeekChange 추가)
  - src/NeisAutoFill.Core/Timetable/TimetableTextNormalizer.cs (StripMarkup 추가)
  - src/NeisAutoFill.Core/Timetable/TimetableGridParser.cs (셀 값에 StripMarkup 적용)
  - tests/NeisAutoFill.Tests/TimetableGridParserTests.cs (실측 HTML 3건)
- 단위 테스트: 325/325 통과 (322 → 325)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: **읽기 전용 전 구간 성공** — 상세는 [실기기검증 §4-A](../maintenance/연간시간표_실기기검증.md)
  - 주차 28개 · 현재 주 56칸 · 경고 0
  - 카탈로그 후보 20 · 명령 9 · 지문 생성
  - 실행 계획 분류: CellUnavailable 2 · AlreadyMatches 1 · Pending 2 · 막힘 0
- 확인된 사실:
  - **CLX 셀 값은 HTML** (`<div></div>과학<br/>(교사(계정))`, 일부는 deeppink `<font>` 로 감쌈).
    태그를 걷어내지 않으면 모든 매칭이 실패한다. 이번 단계에서 가장 큰 발견.
  - **주차 목록 범위(수업일)와 그리드 날짜 범위(월~일)가 다르다.** 셀 주소는 항상 행의 `{day}Ymd` 를 쓴다.
  - CLX 행 읽기는 `toObject()` → `getRowData()` → JSON 복제 순서로 폴백해야 한다(복제만 쓰면 빈 객체가 나온다).
  - `AlreadyMatches` 판정이 실기기에서 성립 — 현재 값과 목표를 안정 키로 비교하는 전제가 검증됐다.
- 실패·제약:
  - deeppink `<font>` 의 의미 미확인 (저장 전 변경 표시 추정)
  - 2주차에서 메뉴가 열리지 않는 이유 여전히 미확인
  - 값이 있는 셀의 우클릭 메뉴는 확인하지 않음
  - NeisTarget/NeisScreenKind 확장과 화면 자동 이동은 아직 없음 — 사용자가 화면을 열어 둔 상태를 전제한다
- 남은 위험: R-006(저장 전후 표현 차이) 열림 — deeppink 가 이것과 관련 있을 수 있다
- 다음 작업: NeisTarget 에 학급시간표관리 추가 + 화면 자동 이동 (담임/전담 회귀 주의, 개발문서 §4.3)
- 되돌리는 방법: TimetableReader.cs 삭제, StripMarkup 호출을 되돌림 (나이스 데이터는 변경 없음)

---

### 2026-08-18 · T4 실기기 읽기 전용 진단(V1~V5) · ✅

- 목표: 로그인된 나이스에서 화면·그리드·메뉴를 읽기 전용으로 실측하고 파서 가정을 검증
- 범위에서 제외: 셀 선택·수업 입력·저장 (전혀 하지 않음). 메뉴는 매번 [취소]로 닫음
- 적용한 결정: D-001, D-004, D-005, D-006, D-012
- 수행 방법: 전용 Edge(CDP 9222)에 Playwright 로 attach, 임시 진단 콘솔로 조회만 수행
- 확인된 사실: 상세는 [실기기검증 §4-A](../maintenance/연간시간표_실기기검증.md) 참고. 요약:
  - 1차 실측과 **전부 일치** (논리 8행, 메뉴 29개=후보 20+명령 9, 스크롤 360/636/276)
  - DOM 에 grdWeekByClsByTi id 는 **없다** → 구조(셀 8개 + 첫 셀 `N교시`)로 특정해야 한다
  - **셀 표시 형식은 `과목⏎(교사(계정))`** 로 메뉴 형식과 다르다 — 같은 안정 키가 나오도록 테스트로 고정
  - 계정에 `#P…` 내부 식별자가 섞인다
  - 복수 교사 실존(과학 3·자율자치 3·도덕/수학/영어 각 2) → D-006 의 실무 근거 확보
  - 학기 시작 전 날짜는 메뉴가 열리지 않는다(사용자 확인) → CellUnavailable 정상 동작
- 변경 파일: tests/NeisAutoFill.Tests/TimetableMenuParserTests.cs (실측 형식 4개 추가)
- 단위 테스트: 322/322 통과 (318 → 322)
- 빌드: 경고 0 · 오류 0
- 실패·제약:
  - **2주차에서는 평일 어디서도 메뉴가 열리지 않았다** — 원인 미확인. 주차별 메뉴 가용성 확인 절차 필요
  - 값이 있는 셀의 우클릭 메뉴는 확인하지 않음
  - 진단은 임시 콘솔로 수행했고 아직 Automation 계층에 편입하지 않았다
- 남은 위험: R-007 열림, R-003 은 완화(복수 교사 실측 확보)
- 다음 작업: 진단 코드를 Automation 계층(TimetableDomInspector/TimetableClxReader)으로 옮기고
  NeisTarget 에 학급시간표관리 추가 — 담임/전담 경로 회귀 주의(개발문서 §4.3)
- 되돌리는 방법: 테스트 4개 제거 (나이스 데이터는 변경한 것이 없음)

---

### 2026-08-18 · T4 부분(행 해석기) · ✅

- 목표: T4 중 브라우저 없이 검증 가능한 조각 — CLX 행 데이터 → 셀 주소 변환
- 범위에서 제외: 화면 이동·메뉴 수집·스크롤·진단 리포트 (실기기 없이는 검증 불가)
- 적용한 결정: D-003
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/TimetableGridParser.cs
  - tests/NeisAutoFill.Tests/TimetableGridParserTests.cs
- 단위 테스트: 318/318 통과 (307 → 318, +11)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: 없음 — 실측 필드명(pir, {day}Ymd, {day}Otpt)만 사용
- 확인된 사실 / 설계 명확화:
  - 파서는 **값만 받는다**(행 = 필드명→값 사전). Automation 계층이 CLX 에서 뽑아 넘기면 되므로
    화면 개편 시 고쳐야 할 곳이 "수집"과 "해석"으로 분리된다.
  - "값이 없는 셀"과 "존재하지 않는 셀"을 구분한다 — 전자는 입력 대상이다.
  - 헤더 날짜 ↔ 행 Ymd 교차 검증(Verify)을 제공한다. 어긋나면 입력을 진행하면 안 된다(V3).
  - 없는 요일 열(토·일 미표시)은 경고하지 않고, 요일이 어긋난 날짜는 경고한다.
- 실패·제약: 요일 필드 접두사(mon~sun)는 실측 1개 환경 기준. 다른 교육청에서 다르면 여기만 고치면 된다
- 남은 위험: R-007 열림
- 다음 작업: **외부 조건이 필요한 단계만 남음** (아래 "현재 막힌 지점" 참고)
- 되돌리는 방법: 위 2개 파일 삭제

---

### 2026-08-18 · T3·T6 코어 · ✅

- 목표: 브라우저·AI 없이 만들 수 있는 나머지 도메인 — 창체 병합 엔진과 실행 계획 생성기
- 범위에서 제외: 문서 파싱(T2, GAS 필요), 나이스 드라이버(T4), 매핑 UI(T5), 입력·저장(T7·T8)
- 적용한 결정: D-002, D-003, D-006, D-008, D-009, D-010, D-011
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/CreativeActivityEvent.cs (창체 일정·병합 결과 모델)
  - src/NeisAutoFill.Core/Timetable/CreativeActivityMerger.cs (정확 병합 / 제안 / 충돌)
  - src/NeisAutoFill.Core/Timetable/TimetableAssignment.cs (실행 분류 12종·계획 집계·주 단위 분할)
  - src/NeisAutoFill.Core/Timetable/TimetablePlanBuilder.cs (원본+매핑+카탈로그+현재값 → 계획)
  - tests/NeisAutoFill.Tests/CreativeActivityMergerTests.cs
  - tests/NeisAutoFill.Tests/TimetablePlanBuilderTests.cs
- 단위 테스트: 307/307 통과 (283 → 307, +24)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: 없음 (순수 로직)
- 확인된 사실 / 설계 명확화:
  - 나이스 현재 상태를 `TimetableScreenState`(셀→값 사전 + 사용 불가 셀 집합) 값으로 받도록 해
    **계획 생성 전체를 브라우저 없이 테스트**할 수 있게 했다. T4 드라이버는 이 값을 채우기만 하면 된다.
  - `CellUnavailable`(휴업일)과 `Skipped`(입력 안 함)은 **실행을 막지 않는 상태**로,
    미해결·충돌 계열만 게이트를 잠근다. 실패와 정상 건너뜀을 섞지 않기 위함이다.
  - 창의 미해결은 `MappingUnresolved` 가 아니라 `CreativeUnresolved` 로 분리했다 — 사용자가 할 일이 다르다.
- 실패·제약:
  - 창체 병합 결과를 실행 계획에 주입하는 연결(창 셀 → 확정된 창체 종류)은 아직 없다.
    창체 매핑을 규칙으로 표현할지 별도 축으로 둘지는 T5 UI 설계와 함께 정해야 한다.
  - 시수 총량 비교(기준 시수 대비)는 미구현 — T6 잔여 항목
- 남은 위험: R-002, R-003, R-006 열림
- 다음 작업: T2 또는 T4. **둘 다 외부 조건이 필요하다**
  - T2: 시간표 전용 GAS 액션 추가·재배포 (사용자 작업)
  - T4: 나이스 로그인 세션에서 읽기 전용 진단 (사용자 PC·계정 필요)
- 되돌리는 방법: 위 4개 소스 파일과 2개 테스트 파일 삭제

---

### 2026-08-18 · T1-b · ✅

- 목표: 셀 주소·매핑 규칙·우선순위 해석·런타임 카탈로그 지문
- 범위에서 제외: 창체 병합(T3), 실행 계획(T6), UI·브라우저 전부
- 적용한 결정: D-001, D-003, D-004, D-006, D-007
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/TimetableCell.cs (날짜+교시 주소)
  - src/NeisAutoFill.Core/Timetable/TimetableMappingRule.cs (적용 범위·규칙·입력 안 함)
  - src/NeisAutoFill.Core/Timetable/TimetableMappingResolver.cs (우선순위 해석·충돌 반환)
  - src/NeisAutoFill.Core/Timetable/TimetableCatalog.cs (후보/명령 분리·안정 키 조회·정렬 독립 지문)
  - tests/NeisAutoFill.Tests/TimetableMappingResolverTests.cs
  - tests/NeisAutoFill.Tests/TimetableCatalogTests.cs
- 단위 테스트: 283/283 통과 (264 → 283, +19)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: 없음 (순수 로직)
- 확인된 사실 / 설계 명확화:
  - **"같은 우선순위 규칙 둘 이상 = 충돌"을 '서로 다른 대상이 둘 이상'으로 좁혔다.**
    대상이 동일한 중복 규칙은 모호하지 않아 막을 이유가 없다(첫 항목을 고르는 것과 다르다).
    D-007 의 취지(임의 선택 금지)는 그대로 유지된다.
  - 카탈로그 지문은 **할당 가능한 항목만** 반영한다. 작업 명령이 늘고 주는 것으로 매핑을 무효화하지 않는다.
  - "입력 안 함"은 예약 키(SkipKey)로 두어 미해결과 구조적으로 구분했다.
- 실패·제약: 저장 포맷(프로필 직렬화)은 아직 없음 — T5 에서 범위(교육청·학교·사용자 해시)와 함께 정의
- 남은 위험: R-003 열림
- 다음 작업: T3 창체 병합·중복 제거 엔진(순수 로직 부분)
- 되돌리는 방법: 위 6개 파일 삭제 (다른 코드가 참조하지 않음)

---

### 2026-08-18 · T1-a · ✅

- 목표: 브라우저 없이 검증 가능한 해석 로직 — 메뉴 문자열 파서, 과목 토큰 정규화, 옵션 모델
- 범위에서 제외: 매핑 규칙·우선순위 Resolver·카탈로그 지문(T1-b), 문서 AI(T2), 나이스 드라이버(T4)
- 적용한 결정: D-001, D-002, D-004, D-006, D-008, D-012
- 변경 파일:
  - src/NeisAutoFill.Core/Timetable/TimetableOption.cs (옵션 종류·창체 종류·안정 키)
  - src/NeisAutoFill.Core/Timetable/TimetableTextNormalizer.cs (비교용 정규화·괄호 짝 검사)
  - src/NeisAutoFill.Core/Timetable/TimetableMenuParser.cs (메뉴 문자열 해석)
  - src/NeisAutoFill.Core/Timetable/TimetableTokenNormalizer.cs (별칭 → 표준 의미)
  - tests/NeisAutoFill.Tests/TimetableMenuParserTests.cs
  - tests/NeisAutoFill.Tests/TimetableTokenNormalizerTests.cs
- 커밋: (아래 커밋 해시)
- 단위 테스트: 264/264 통과 (221 → 264, 시간표 43개 추가)
- 빌드: 경고 0 · 오류 0
- 실기기 검증: 없음 (순수 로직 단계 — 나이스 접속·쓰기 없음)
- 확인된 사실:
  - **NFKC 가 'ㆍ'(U+318D)를 U+119E 로 바꾼다.** 가운뎃점 목록에 둘 다 넣지 않으면
    "자율ㆍ자치활동"이 창체로 인식되지 않는다. 정규화 순서에 의존하지 않도록 NFKC 앞뒤로 모두 처리했다.
  - 괄호 짝이 안 맞는 항목은 과목명으로 통과시키면 안 된다 → HasBalancedParens 로 걸러 Unknown 처리.
    "괄호 없음"(과목만)과 "괄호 깨짐"은 다른 상태다.
- 실패·제약:
  - 창체 활동명 3종(자율·자치활동/동아리활동/진로활동)은 실측 1개 환경 기준. 다른 교육청 표기가 다르면 보강 필요
  - 작업 명령 목록도 실측 1개 환경 기준 — 개수를 상수로 쓰지 않으나 이름은 사전 비교라 미지의 명령은 Unknown 이 아니라 Lesson 으로 샐 수 있다(T4 진단에서 재확인)
- 남은 위험: R-003, R-007 (환경별 카탈로그·표기 차이) 열림 유지
- 다음 작업: T1-b — TimetableMappingRule/적용 범위, 날짜·교시 > 요일·교시 > 요일 > 기본값 Resolver, 동일 우선순위 충돌 반환, 카탈로그 지문
- 되돌리는 방법: src/NeisAutoFill.Core/Timetable/ 과 tests/NeisAutoFill.Tests/Timetable*.cs 삭제 (다른 코드가 참조하지 않음)

---

### 2026-08-18 · T0 · ✅

- 목표: 연간 시간표 자동입력 요구와 실제 나이스 동작을 구현 가능한 기준으로 고정
- 결정: D-001~D-012
- 확인한 기존 코드:
  - Generator/PlanFileExtractor.cs
  - Generator/GasPlanImporter.cs
  - Core/SubjectMapper.cs
  - App/Helpers/MatchSession.cs
  - App/ViewModels/MatchPreviewViewModel.cs
  - Automation/NeisEngine.cs
  - Automation/ClxGridApi.cs
  - Automation/DomInspector.cs
- 실기기 검증:
  - 학급시간표관리와 주차 변경
  - 빈 셀 우클릭 메뉴
  - 메뉴 전체 항목과 실제 스크롤
  - 수업 없는 날의 메뉴 미표시
  - 메뉴 재개방 시 UUID 변경
  - 취소로 종료, 셀 선택·저장은 하지 않음
- 확인된 사실:
  - 대상 그리드 ID grdWeekByClsByTi
  - 논리 구조 1~8교시 × 월~일
  - 메뉴는 별도 오버레이이며 수업 후보와 작업 명령이 섞여 있음
  - 모든 항목은 DOM에 있으나 화면 밖 항목 선택에는 메뉴 스크롤이 필요
- 변경 파일:
  - docs/architecture/연간시간표_기술설계.md
  - docs/roadmap/연간시간표_자동입력_로드맵.md
  - docs/maintenance/연간시간표_실기기검증.md
  - README.md 문서 인덱스
- 커밋: 없음
- 코드·테스트: 변경 없음
- 다음 작업: T1 Core 모델과 순수 단위 테스트
- 되돌리는 방법: 위 문서와 README 인덱스 항목만 제거

---

## 16-A. 현재 막힌 지점 (2026-08-18)

브라우저·AI 없이 만들 수 있는 도메인은 모두 끝났다. 남은 단계는 **사용자만 풀 수 있는 외부 조건**을 기다린다.

| 단계 | 필요한 것 | 누가 |
|---|---|---|
| T2 문서 인식 | `code.gs` 에 시간표 전용 액션(예: `parseTimetable`) 추가 후 **Apps Script 재배포**. `parsePlan` 은 평가계획 스키마 전용이라 재사용 불가 | 사용자 (저장소 밖) |
| T2 검증 | 실제 연간 시간표 PDF/HWP/HWPX 샘플 (비식별) | 사용자 |
| T4 나머지 | 로그인된 나이스에서 읽기 전용 진단 (V1~V6) — 화면 이동·메뉴 수집·스크롤 실측 | 사용자 PC·계정 |
| T5·T7·T8 | T2·T4 결과 위에서만 진행 가능 | — |

준비된 것: 원본 해석·매핑 규칙·창체 병합·실행 계획·행 데이터 해석 (테스트 97개).
T4 드라이버는 `TimetableScreenState` 와 `TimetableGridParser` 에 값을 채워 넣기만 하면 계획 생성까지 바로 이어진다.

---

## 17. 다음 개발자 시작점

가장 먼저 T1만 구현한다. UI나 브라우저 자동화를 동시에 시작하지 않는다.

권장 첫 작업 단위:

1. TimetableOptionKind와 NeisTimetableOption
2. 특수 명령을 우선 제외하는 메뉴 문자열 파서
3. TimetableTokenNormalizer
4. TimetableMappingRule과 충돌을 드러내는 Resolver
5. 위 네 항목의 xUnit 테스트

첫 작업이 끝나면 T1 체크리스트와 진행 기록을 갱신하고, 전체 테스트가 통과한 뒤 T2 또는 T4 중 하나를 시작한다. T2와 T4는 서로 독립적으로 진행할 수 있지만 T5는 둘 다 끝난 뒤 시작한다.

---

## 18. 진행 기록 템플릿

~~~markdown
### YYYY-MM-DD · Tn · 상태

- 목표:
- 범위에서 제외:
- 적용/변경한 결정:
- 변경 파일:
- 커밋:
- 단위 테스트:
- 빌드:
- 실기기 검증:
- 확인된 사실:
- 실패·제약:
- 남은 위험:
- 다음 작업:
- 되돌리는 방법:
~~~
