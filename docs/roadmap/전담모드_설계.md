# 전담 모드 설계·개발 로드맵 (F9)

전담 교사가 **여러 학년·반의 특정 과목**을 가르치는 상황을 지원하기 위한 설계.
담임 모드(현재)는 그대로 두고, 전담 모드를 **별도 데이터 구조**로 얹는다.

> 상태: **설계 확정용 문서 (아직 미구현)**. Phase A(경로 인프라)만 커밋됨.
> 이 문서는 나중에 그대로 AI 에게 주고 "이 로드맵대로 M2 부터 개발해줘" 라고 시킬 수 있게 자세히 적었다.
> 작성: 2026-07-18

---

## 1. 문제 — 담임과 전담은 데이터 모양이 다르다

| | 담임 (현재) | 전담 |
|---|---|---|
| 명단 | 우리 반 하나 | **반마다** 다름 (3-1, 3-2, 4-1…) |
| 평가계획 | 과목별 (우리 반 전 과목) | **(학년·과목)마다** 다름. 같은 학년·과목이면 여러 반이 공유 |
| 성적·서술문 | 과목별 (한 반) | **(학년·반·과목) 조합마다** |

핵심 통찰: 전담의 데이터는 **세 개의 축**으로 정규화된다.
- **명단** = `(학년, 반)` 의 함수
- **평가계획** = `(학년, 과목)` 의 함수 — 반은 무관 (3-1 영어와 3-2 영어의 평가계획은 같다)
- **작업(성적·서술문)** = `(학년, 반, 과목)` = 명단 × 평가계획의 교차점

→ 담임 구조("한 파일에 과목 시트들")에 억지로 담으면 명단·계획이 반 수만큼 중복된다.

---

## 2. 설계 결정 — 분리 저장(정규화). 담임 파일과 전담 파일은 완전히 따로

### 왜 분리 저장인가 (통째 복사 대신)
- **현실 반영**: 3-1·3-2가 같은 "3학년 영어 평가계획"을 공유하는 게 실제 모습. 통째 복사면 같은 계획을 반 수만큼 다시 입력해야 한다.
- **AI 통합 분석과 자연스럽게 연결**: "평가계획을 한 파일에 다 넣고 AI가 학년·과목별로 분리" 요구(§5)가 곧 `(학년·과목)` 단위 저장으로 귀결된다.
- **수정 편함**: 계획 하나 고치면 그 학년 전 반에 반영. 명단도 반별 1회 입력으로 그 반의 전 과목이 공유.
- **단점**(감수): 구조가 복잡하고 개발이 크다. 하지만 실사용 가치가 훨씬 크다.

### 폴더 레이아웃
```
문서\NeisAutoFill\
 ├ 성적.xlsx  평가계획서.xlsx  서술문.xlsx        ← 담임(현재). 전담 켜기 전까지 안 건드림
 └ 전담\
    ├ 명단\        {학년}-{반}.xlsx               ← 반별 학생명단 (번호·이름)   예: 3-1.xlsx
    ├ 평가계획\    {학년}학년.xlsx                ← 학년별 1파일(과목=시트, 담임 포맷)  예: 3학년.xlsx
    └ 작업\        {학년}-{반}_{과목}\            ← 조합별 결과                 예: 3-1_영어\
                     성적.xlsx  서술문.xlsx
```
- 내부 데이터(narratives.json·state.json)는 `%AppData%\NeisAutoFill\profiles\{조합}\` (Phase A 의 `ProfilePaths.DataFile` 이미 지원).
- **담임 무영향**: `전담\` 폴더는 전담 모드를 켜야만 생긴다. 담임 사용자는 기존 경로 그대로 (Phase A 의 `ProfilePaths.IsDefault`).

---

## 3. 사용 흐름 (★ 확정 — 조합 미리 등록 안 함)

전담은 **명단·계획만 등록**하고, 메인에서 **(학년·반·과목)을 그때그때 바꾸면** 등록된 것에서 **자동 매칭**해 성적표를 만든다.
담임의 "과목 탭 전환"에 "학년·반 전환"이 더해진 형태.

```
[설정]        담임 / 전담  ← 모드 스위치만. 반·과목 등록 안 함

[자료 준비]   (전담일 때)
  · 반별 학생명단 등록        → 명단[(학년,반)]      예: 3-1 명단, 3-2 명단
  · (학년·과목)별 평가계획 등록 → 평가계획[(학년,과목)]  예: 3학년 영어 계획
      (평가계획은 한 파일에 몰아 넣고 AI 가 학년·과목별로 분리 인식 — §5)

[메인 화면]   (전담일 때)
  · 상단에 [학년][반] 콤보  +  과목 탭 (담임처럼)
  · (학년,반,과목) 을 고르면 → 명단[(학년,반)] + 평가계획[(학년,과목)] 자동 매칭
      → 그 조합의 성적표가 뜬다 → 등급·서술문 입력 (담임과 동일한 편집·입력)
  · 명단/계획이 없는 조합이면 → "이 반 명단이 없습니다 / 이 학년·과목 계획이 없습니다" 안내
```

### 메인의 학년·반·과목 선택지는 어디서 오나 (파생, 미리 등록 X)
- **학년·반 콤보** = 등록된 **명단 파일**들의 (학년,반) 목록 (명단 폴더 스캔).
- **과목 탭** = 등록된 **평가계획 파일**들 중 현재 학년의 과목 (계획 폴더 스캔).
- → 조합을 명시적으로 등록하지 않는다. **파일(명단·계획)이 있으면 곧 선택 가능**.
- (M2 의 `SubjectAssignment`(조합 미리 등록) 는 이 방향에서 **불필요** — 파일 스캔으로 대체. M2 코드는 남기되 미사용/추후 정리.)

---

## 4. 담임 vs 전담 모드 전환

- 설정 › 일반에 **담임 / 전담** 라디오. **모드만** 바꾼다(반·과목 등록은 여기서 안 함).
- 전담으로 바꾸면: 자료 준비에 전담 명단·계획 등록 UI 가 열리고, 메인 상단에 학년·반 콤보가 생긴다.
- **전환 방식 확정**: 모드(담임↔전담) 전환은 **재시작**(드묾·안전 — 경로 싱글턴 재구성 회피). 현재 (학년·반·과목) 조합 전환은 **인플레이스**(전담이 수시로 바꿈 — 담임의 과목 탭 전환처럼 매끄럽게, M5).
- 설정 저장 위치: `profiles.json` (mode + 현재 학년·반·과목). 공용(프로필 무관).

---

## 5. AI 통합 평가계획 분석 (학년·과목 자동 분리)

전담은 담당하는 여러 (학년·과목)의 평가계획을 **한 파일(또는 몇 개)에 몰아넣고**, AI 가 알아서 쪼개길 원한다.

### 현재 (담임, 2단계 파싱 — GasPlanImporter)
1. `listPlanSubjects`: 문서에서 **과목명** 목록 발견
2. `parsePlan(onlySubject)`: 과목별로 나눠 완전 추출

### 확장 (전담 — 학년 축 추가)
1. `listPlanUnits`(신규): 문서에서 **(학년, 과목)** 쌍 목록 발견
   - 프롬프트에 "학년"도 인식하도록. 예: "3학년 영어", "4학년 과학"
   - 문서에 학년 표기가 없으면(한 학년치만) → 사용자가 고른 학년으로 귀속하거나 담임식 폴백
2. `parsePlanUnit(onlyGrade, onlySubject)`(확장): (학년·과목)별 완전 추출
3. 결과를 `평가계획\{학년}학년.xlsx` 로 저장 (과목=시트, 담임 평가계획서 포맷 재사용) → 해당 반들이 참조

### 구현 메모
- `GasPlanImporter` 의 `ListSubjectsAsync`/`ParseSubjectAsync` 에 `grade` 파라미터 추가, 또는 "단위(unit)" 개념으로 일반화.
- code.gs 의 `listPlanSubjects`/`parsePlanDocument` 프롬프트에 학년 인식 지시 추가. (셀렉터와 달리 프롬프트는 GAS 재배포만으로 갱신 가능)
- **하위호환**: 담임 경로(과목만)는 그대로 두고, 전담 경로에서만 학년 축을 쓴다. 또는 grade=null 이면 현재 동작.

---

## 6. 개발 마일스톤 (단계별 — 각 단계 독립 커밋·검증)

| M | 내용 | 위험 | 실기기 |
|---|---|---|---|
| **M0** ✅ | 경로 인프라 (ProfilePaths·AppPaths·ProfileStore). 담임 기본=기존 경로 (Phase A, 완료) | 낮음(동작무변화) | 불필요 |
| **M1** ✅ | 데이터 계층: 도메인 모델(ClassRef·TeachingUnit)·경로 규칙(SubjectModePaths)·조합 조립(BuildUnitSheet). 순수 로직 테스트 15개 | 중 | 불필요 |
| **M2** ⚠️ | (방향 수정됨) 조합 미리 등록(SubjectAssignment)은 **폐기 방향** — 파일 스캔으로 대체. ProfileStore 는 mode+현재조합만. 코드는 남기되 미사용 | — | — |
| **M3** ✅ | 설정: 담임/전담 **모드 스위치만**(반·과목 등록 없음). 모드 변경 시 재시작. 담임 무영향 | 낮음 | 앱 확인 |
| **M4a** ✅ | 자료 준비(전담): 왼쪽 명단에 반 콤보+[＋학년·반](반별 명단 저장/전환), 오른쪽 계획에 학년 콤보+[＋](학년 공유). SubjectModeStore 로 전담\ 폴더에 저장. 담임 무영향 | 중 | 앱 확인 |
| **M4b** OK | AI 통합 분석: 한 파일→ (학년·과목) 분리 인식(listPlanUnits + parsePlan onlyGrade) + 선택 창. 학년별 파일 병합 저장. 담임엔 과목 선택 창 | 중 | GAS 재배포 |
| **M5** OK | 메인 축 전환: 학년·반 분리 콤보 + [이동] 버튼(선택만으론 안 바뀜 — 원치 않는 전환 방지). 명단+계획 합쳐 성적표, narratives/성적 조합별 경로. 인플레이스(재시작 없이) | 높음 | 앱 확인 |
| **M6** | 나이스 연결: 전담이 반·과목 바꿔가며 조회·입력. 백업/복원·초기화 전담 인지, 문서·실사용 검증 | 높음 | **실기기 필수** |

### 진행 원칙
- **M1·M2 는 순수 로직 위주** → 테스트로 덮고 안전하게. 여기서 데이터 모델을 확정.
- **M3 이후 UI·엔진**은 실기기 확인 전제. 특히 **M5(나이스 조회·입력)** 는 전담이 반·과목을 바꿔가며 조회하므로 실기기 검증이 반드시 필요.
- 각 M 마다 **담임 모드 회귀 없음**을 확인(담임은 M0 경로로 계속 동작).

---

## 7. 현재 코드에서 재활용 / 변경 지점

| 항목 | 지금 | 전담에서 |
|---|---|---|
| `ProfilePaths`(Core) | 프로필별 경로 규칙 (M0) | 그대로 활용 — 조합명을 프로필로 |
| `AppPaths.CurrentProfile` | 현재 프로필 반영 | 현재 조합 반영 |
| `ProfileStore` | mode·current·sets (단순) | **M2 에서 3층 등록 모델로 재작성** |
| `PlanWorkbookLoader` | 과목 시트+[학생명단] | 명단·계획 분리 로드 API 추가(M1) |
| `WorkspaceService` | 담임 자료 수명 | 조합 자료 수명으로 일반화(M1·M5) |
| `GasPlanImporter` | 과목 2단계 파싱 | 학년 축 추가(M4) |
| Phase B 세트 UI | 되돌림(단순 세트라 실요구와 불일치) | M3 에서 3층 모델로 새로 |

---

## 8. 리스크 · 미결정

- **나이스 조회 단위**: 전담이 반·과목을 바꿀 때 나이스 화면 전환이 담임과 다른지 실기기로 확인 필요(M5).
- **학년 표기 인식**: 평가계획 문서에 학년이 명확히 안 적혀 있으면 AI 분리가 애매. 폴백(사용자 지정) 필요(M4).
- **조합 폭발**: 담당이 많으면 조합이 수십 개. UI 가 감당하는지(목록·검색)(M3).
- **명단·계획 수정 파급**: 계획 하나 고치면 그 학년 전 반의 성적표에 영향 — 재계산·보존 규칙(M1, 담임의 "영역 개명 보존" 로직 참고).
- **창의적 체험활동·행동특성**(향후): 담임이 프로필 안에서 추가 입력 영역으로. 이 구조가 그릇이 됨 — `ProfilePaths.DataFile` 이 파일명 파라미터라 같은 폴더에 `창체.json` 등 공존 가능. 전담 설계와 독립적.

---

## 9. 지금까지 (진행 기록)

| 날짜 | 내용 |
|---|---|
| 2026-07-18 | **M0 완료** — 경로 인프라(ProfilePaths·AppPaths·ProfileStore). 담임 기본=기존 경로, 동작 무변화. 테스트 8개 |
| 2026-07-18 | Phase B(단순 세트 UI) 시도 후 되돌림 — 실요구(명단 반별·계획 학년과목별 정규화)와 불일치. 이 문서로 설계 재정립 |
| 2026-07-18 | **M1 완료.** 전담 도메인·경로 규칙·조합 성적표 조립(순수 로직, 테스트 15개). 병렬 테스트 플레이키(NeisSelectors 정적 상태 공유) 함께 수정 |
| 2026-07-19 | **M4b 완료(클라이언트).** 평가계획 넣으면 AI가 과목 파악→ **선택 창**에서 고름. 담임=과목만(ImportAsync+selectSubjects, 기존 GAS 재사용). 전담=별도 메서드 ImportUnitsAsync: (학년·과목) 분리 인식(GAS listPlanUnits 신규)→학년 불명은 창에서 지정→학년별 {학년}학년.xlsx 에 병합 저장(다른 과목 보존). parsePlan onlyGrade 추가. PlanPickerWindow 신설. ★code.gs 재배포 필요 |
| 2026-07-18 | **M5 완료.** 메인 학년·반 분리 콤보+[이동](원치 않는 전환 방지), 인플레이스 전환(NarrativeStore.SwitchTo·조합별 성적경로). 자료준비: 반 제거·오름차순·중복안내, 학년=계획∪반(빈 계획도 학년 유지 버그 수정). 담임 무영향 |
| 2026-07-18 | **M4a 완료.** 자료 준비 전담 UI — 왼쪽 반 콤보+추가(반별 명단), 오른쪽 학년 콤보+추가(학년 공유 계획), 전환 시 자동 저장/로드(SubjectModeStore). 학년·반 추가 대화상자. 담임 무영향. 앱 확인 |
| 2026-07-18 | **M4a 착수(설계 확정).** 자료준비 전담 확장: 왼쪽 명단에 학년·반 콤보+추가버튼(반별), 오른쪽 계획은 학년 공유. **계획=학년별 1파일(과목 시트)** 로 결정(담임 PlanWorkbookLoader/Writer 재사용, 유지보수·성능·AI분리 정합). SubjectModePaths.PlanFile 을 학년별로 조정, 테스트 갱신 |
| 2026-07-18 | **M3 완료.** 설정에 담임/전담 모드 스위치. 모드 변경 시 재시작(경로 싱글턴 안전). 전환 방식 확정: 모드=재시작, 조합=인플레이스(M5). 담임 무영향 |
| 2026-07-18 | **M2 완료(후 방향 수정).** SubjectAssignment(조합 미리 등록) 만들었으나, 실요구 확인 결과 **조합을 미리 등록하지 않고** 명단·계획만 등록→메인에서 (학년·반·과목) 자동 매칭하는 흐름으로 확정. M3 설정 등록표 UI 되돌림. 로드맵 §3·§4·마일스톤 재작성. SubjectAssignment 는 미사용으로 남김 |
