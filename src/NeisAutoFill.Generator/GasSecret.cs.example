// 이 파일을 같은 폴더에 GasSecret.cs 로 복사한 뒤 실제 시크릿을 넣으세요.
// GasSecret.cs 는 .gitignore 로 저장소에서 제외됩니다 (공개 저장소에 시크릿 노출 방지).
// 값은 GAS code.gs 의 V2_SECRET 과 반드시 일치해야 합니다.
namespace NeisAutoFill.Generator;

public static partial class GasAuth
{
    static partial void LoadSecret(ref string secret) => secret = "여기에_실제_시크릿";
}
