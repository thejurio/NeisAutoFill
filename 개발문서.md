# NEIS 교과평가 자동입력기 — 개발 문서

최종 업데이트: 2026-07-08 (v1.2.0)
저장소: https://github.com/thejurio/NeisAutoFill
규모: 소스 73파일 / 약 4,800라인, 단위 테스트 55개

---

## 1. 이 프로그램이 하는 일

초등교사가 엑셀에 정리한 성적을 4세대 나이스에 자동 입력하는 도구.

```
[평가계획서]  영역·성취기준·평가기준           (엑셀)
     │
[성적 채점]   학생 × 영역 → 등급 (사용자정의 척도)  (엑셀, 프로그램에서 편집도 가능)
     │
[AI 서술문]   교과학습발달상황 서술문 생성 (Gemini, GAS 경유)
     │
[나이스 입력]
     ├ 등급 드롭다운  「교과별 평가」        ← 검증 완료
     └ 서술문 텍스트  「학기말 종합의견」    ← 셀렉터 실측 완료
```

**핵심 안전 정책** (전 기능 공통, 절대 변경 금지)
- 나이스 [저장] 버튼은 자동으로 누르지 않는다 — 최종 확정은 반드시 사람이
- 화면에 실제로 그려진 학생 이름·번호를 읽어 매칭 (추측 입력 없음)
- 로그인 자동화 안 함 — 사용자가 로그인한 Edge에 CDP로 attach
- 입력 후 값 재검증 + 1회 재시도, 실패는 사유와 함께 리포트

---

## 2. 개발 연혁 (릴리스 기준)

| 버전 | 날짜 | 내용 |
|---|---|---|
| — | 2026-07-07 | Python+Selenium 프로토타입 검증 완료 (개발노트 인수인계) |
| v1.0.0 | 07-07 | C# 재구현 — 등급 입력·AI 생성·서술문 입력·양식·지역선택·자동업데이트 |
| v1.0.1 | 07-07 | 자동업데이트 기본 활성화 |
| v1.0.2 | 07-07 | 자동업데이트 실동작 검증 (구→신 버전 교체 테스트 통과) |
| v1.0.3 | 07-07 | 앱 아이콘 (성적 문서 + 완료 체크) |
| v1.1.0 | 07-07 | UI 전면 개편(NEIS 톤 커스텀 타이틀바) + 성적 표 편집 + 자동 연결 |
| v1.2.0 | 07-08 | 하이브리드 매칭(이름→순서 폴백) + 서술문 삭제 + 오류 팝업 |

### 개발 중 규명한 핵심 사실
- **§8 "마지막 행 미렌더링"의 진짜 원인**: 개발노트의 가상스크롤 가설은 틀렸음.
  실기기 DOM 덤프 결과, 마지막 행만 aria-label에 `"마지막 행"` 토큰이 추가됨
  (`"17행 마지막 행 성명 …"`). 정규식이 이를 못 읽어 누락됐던 것. → 정규식에 `(?:마지막 행 )?` 추가로 해결.
- **서술문 화면 편집 컨트롤**: 네이티브 `<textarea>`가 아니라 CLX 커스텀 `div.cl-textarea`.
  클릭하면 편집 모드로 진입해 실제 textarea가 동적 생성됨 (콤보 팝업과 같은 패턴).
- **CLX 공식 스크롤 API**: `cpr.core.Platform.INSTANCE.getAllRunningAppInstances()` 로
  그리드 컨트롤을 찾아 `reveal(rowIndex)` 호출 가능 (누락 행 복구 폴백으로 사용).
- **API 키는 GAS 시트에서 관리** — 프로그램에 키 입력 UI를 두지 않는다 (사용자 결정).
- **화면 요동 복구 금지** — 휠 연타/PageDown 등 화면을 떨리게 하는 복구는 제거 (사용자 결정).

---

## 3. 솔루션 구조 (5계층 + 테스트)

```
NeisAutoFill.sln
├─ src/NeisAutoFill.Core/         도메인 (외부 의존성 0 — 순수 C#, 테스트 대부분 여기)
├─ src/NeisAutoFill.Excel/        엑셀 읽기·쓰기 (ClosedXML)
├─ src/NeisAutoFill.Automation/   브라우저 엔진 (Playwright, CDP attach)
├─ src/NeisAutoFill.Generator/    AI 서술문 생성 (GAS 백엔드 호출)
├─ src/NeisAutoFill.App/          WPF UI (MVVM)
└─ tests/NeisAutoFill.Tests/      xUnit 55개
```

의존성 방향 (안쪽으로만): `App → {Automation, Excel, Generator} → Core`

---

## 4. 파일별 역할

### 4.1 Core — 도메인 (의존성 0)

**Models/** — 불변 record
| 파일 | 역할 |
|---|---|
| `Student.cs` | 엑셀 학생 한 명: No, Name, Grades(영역→등급), SpecialNote |
| `SubjectSheet.cs` | 한 과목: SubjectName, Areas[], Students[] |
| `SubjectPlan.cs` | 평가계획서 한 과목: 영역·(영역,등급)→평가기준(criteria) |
| `DomainPoint.cs` | AI 생성에 넘길 한 영역 성취내역 (영역·등급·기준·성취기준) |
| `RowMeta.cs` | 나이스 그리드 한 행 파싱 결과 (No, Name, Area) |
| `GradeTask.cs` | 입력할 한 건 (RowIndex, No, Name, Area, TargetGrade) |
| `NarrativeEntry.cs` | 나이스에 넣을 서술문 (No, Name, Text) + NarrativeReport |
| `RunReport.cs` | 실행 결과 (Done/Skipped/Failed/Missing) + SkipItem |

**Matching/** — 매칭·정규화 로직 (핵심, 전부 테스트됨)
| 파일 | 역할 |
|---|---|
| `NameNormalizer.cs` | 이름 정규화 — "박서연(전입학)"→"박서연" |
| `StudentMatcher.cs` | ★ 화면 행↔엑셀 매칭. **하이브리드**: 이름 기반 기본, 문제 시 순서 기반. DetectNameProblem으로 판정, 순서 정렬 시 개수 다르면 FatalError |
| `NarrativeMatcher.cs` | 서술문 화면 행↔생성 서술문 매칭 |
| `EvaluationAssembler.cs` | 학생 등급 + 평가계획 기준 → DomainPoint 목록 조립 |

**Scale/** — 사용자 정의 평가척도
| 파일 | 역할 |
|---|---|
| `GradeScale.cs` | 척도(이름+레벨목록), GradeLevel(라벨+AI뉘앙스) |
| `GradePresets.cs` | 내장 프리셋 (3단계/상중하/4·5단계) — 편집 시작점 |
| `IScaleStore.cs` / `JsonScaleStore.cs` | 척도 저장/로드 (scales.json). 항상 "내 평가척도" 하나로 관리 |

**기타**
| 파일 | 역할 |
|---|---|
| `NeisRegions.cs` | 17개 시도 교육청 나이스 주소 ({코드}.neis.go.kr) |
| `NarrativeStore.cs` | 생성된 서술문 영속화 (narratives.json), 키=(과목,번호,이름) |
| `TextMetrics.cs` | 서술문 UTF-8 바이트 계산 (나이스 글자수 제한 검사) |

### 4.2 Excel — 엑셀 입출력 (ClosedXML)

| 파일 | 역할 |
|---|---|
| `WorkbookLoader.cs` | 성적파일 읽기 (번호\|이름\|영역들\|특기사항) |
| `PlanWorkbookLoader.cs` | 평가계획서 읽기 (영역·성취기준·기준내용, 학생명단). LooksLikePlan으로 파일 종류 자동판별 |
| `TemplateWriter.cs` | 양식 생성 (평가계획서 빈 양식 / 성적입력 양식) |
| `GradeWorkbookWriter.cs` | 편집된 성적 되저장 |
| `NarrativeWorkbookWriter.cs` | 생성된 서술문 엑셀 내보내기 (과목별 시트) |

### 4.3 Automation — 브라우저 엔진 (Playwright)

| 파일 | 역할 |
|---|---|
| `NeisSelectors.cs` | ★ 모든 CSS 셀렉터·정규식·타이밍 상수 집중 (나이스 DOM 변경 시 여기만 수정). 마지막 행 '마지막 행' 토큰 대응 정규식 포함 |
| `EngineOptions.cs` | 엔진 설정 (Edge 경로·포트·나이스 URL) |
| `EdgeLauncher.cs` | 디버그 포트로 Edge 실행 (--remote-debugging-port=9222) |
| `Abstractions/INeisEngine.cs` | 엔진 인터페이스 (ViewModel이 의존) |
| `NeisEngine.cs` | ★ 엔진 본체 — attach/생존확인/과목검증/등급입력/서술문입력. 하이브리드 매칭 모드 결정·확인 콜백 |
| `RowMapBuilder.cs` | 행 지도 작성 — 프록시 스크롤로 훑으며 {rowindex→행메타} 수집. 누락 시 CLX reveal 복구. 진단 로그(행지도 덤프) |
| `ComboBoxDriver.cs` | 커스텀 단계 콤보 조작 (네이티브 클릭→옵션 폴링→선택). JS클릭 금지 |
| `GridScroller.cs` | 가상 스크롤 제어 (프록시 스크롤바 scrollTop) — 그리드 스코프 한정 |
| `ClxGridApi.cs` | CLX 프레임워크 reveal(rowIndex) 호출 (누락 행 복구 폴백) |
| `NarrativeWriter.cs` | 서술문 입력 — div.cl-textarea 클릭→동적 textarea 획득→입력→재검증 |
| `DomInspector.cs` | 화면 구조 진단 도구 (UI에선 제거, 코드 유지 — docs/보관_진단_검증도구.md) |

### 4.4 Generator — AI 서술문 (GAS 백엔드)

| 파일 | 역할 |
|---|---|
| `IEvaluationGenerator.cs` | 생성기 인터페이스 |
| `GasBackendGenerator.cs` | GAS 웹앱 doPost 호출 (유일한 경로). API 키는 GAS의 APIKeys 시트에서 관리 |
| `GeneratorOptions.cs` | 설정 (GAS URL, 바이트제한, 지역코드, 업데이트 저장소) |
| `PromptBuilder.cs` | 등급→AI 뉘앙스 해석 (프롬프트 본문은 GAS 쪽 code.gs) |

### 4.5 App — WPF UI (MVVM, 시안 C 디자인)

**진입/공통**
| 파일 | 역할 |
|---|---|
| `App.xaml(.cs)` | DI 부트스트랩, Theme.xaml 병합, 전역 크래시 로거, 자동업데이트 시작 |
| `Theme.xaml` | ★ 공통 스타일 (NEIS 블루 #3175FF, 맑은 고딕, 버튼·콤보·탭·그리드·타이틀바 버튼) |
| `GradeBadgeConverter.cs` | 등급→배지 색 (상=초록/중=노랑/하=빨강) |
| `Mvvm/ObservableObject.cs` | INotifyPropertyChanged 기반 |
| `Mvvm/RelayCommand.cs` | ICommand (동기/비동기 재진입 방지) |

**창 (전부 커스텀 타이틀바 + 무테)**
| 파일 | 역할 |
|---|---|
| `MainWindow.xaml(.cs)` | 메인 — 툴바/과목탭/성적표(편집·배지)/진행바/로그. 등급 컬럼 배지↔드롭다운 생성, 저장확인, 창버튼 |
| `GeneratorWindow.xaml(.cs)` | 교과학습발달상황 생성 창 |
| `DataPrepWindow.xaml(.cs)` | 자료 준비 팝업 (양식 저장·평가계획서 열기) |
| `ScaleEditorWindow.xaml(.cs)` | 평가척도 설정 (단계 수·이름·AI뉘앙스) |

**ViewModel**
| 파일 | 역할 |
|---|---|
| `MainViewModel.cs` | ★ 메인 로직 — 파일 로드/양식, 등급 입력 실행(하이브리드 확인·결과 팝업), 자동연결 루프, 지역/척도, 성적 저장 |
| `SubjectViewModel.cs` | 한 과목 탭 — 편집 가능 DataTable, 표에서 Sheet 재구성(편집 반영), Dirty 추적 |
| `GeneratorViewModel.cs` | 서술문 생성/삭제/업로드/엑셀저장, 학생별 항목(StudentGenItem), 결과 영속화·복원 |
| `ScaleEditorViewModel.cs` | 척도 편집 (레벨 추가/삭제/이동, 저장검증) |

**Services**
| 파일 | 역할 |
|---|---|
| `AppPaths.cs` | %AppData%\NeisAutoFill\ 경로 (scales/settings/narratives.json) |
| `GeneratorSettingsStore.cs` | 생성기 설정 영속화 |
| `UpdateService.cs` | GitHub 릴리스 자동업데이트 (최신 태그 비교→다운로드→교체 스크립트→재시작) |

---

## 5. 핵심 알고리즘

### 5.1 하이브리드 등급 매칭 (v1.2.0, StudentMatcher)
```
1. DetectNameProblem: 엑셀 영역명 중복 / 나이스 같은영역 중복 / 매칭 안 되는 영역명 검사
2. 문제 없음 → 이름 기반 (영역명으로 매칭, 순서 무관)  ← 기본·안전
3. 문제 있음 → 사용자에게 "순서대로 입력할까요?" 확인
     [예] → 순서 기반 (학생별 나이스 행 순서 ↔ 엑셀 영역 순서 위치 정렬)
             단, 영역 개수 다르면 FatalError → 중단 (오입력 방지)
     [아니오] → 중단
```

### 5.2 등급 입력 흐름 (NeisEngine.RunSubjectAsync)
```
attach 확인 → 화면 과목==대상 검증 → 그리드 탐색
→ RowMapBuilder(행 지도) → StudentMatcher(하이브리드) → todo
→ todo 순회: 행 가시화 → 콤보 열기 → 옵션 클릭 → 재검증 → 1회 재시도
→ 리포트 (성공/건너뜀/실패/누락) → 문제 있으면 팝업 안내
저장은 안 함
```

### 5.3 마지막 행 처리 (RowMapBuilder)
```
프록시 스크롤로 위→아래 훑으며 렌더된 행 스캔 (idx 키, 중복 스킵)
→ 누락 있으면 CLX reveal(idx) 폴백 → 재스캔
→ 진단: 행 지도 전체(rowindex→영역) + 파싱실패 원본 라벨 로그
(정규식이 '마지막 행' 토큰을 처리하므로 실제 누락은 거의 없음)
```

### 5.4 자동 연결 (MainViewModel.AutoConnectLoopAsync)
```
앱 시작부터 3초 주기 백그라운드 루프:
  미연결 → attach 조용히 시도 (이미 열린 브라우저 자동 포착)
  연결됨 → IsAliveAsync 확인 → 끊겼으면 재연결
UI 갱신은 Dispatcher로 마셜링, 엔진 호출은 백그라운드 (UI 안 멈춤)
```

---

## 6. 데이터·설정 파일

| 파일 | 위치 | 내용 |
|---|---|---|
| scales.json | %AppData%\NeisAutoFill\ | 평가척도 (활성 척도·레벨) |
| settings.json | 〃 | 생성기 설정 (GAS URL·지역·바이트제한·업데이트저장소) |
| narratives.json | 〃 | 생성된 서술문 (과목,번호,이름→텍스트) |
| crash.txt | 〃 | 전역 예외 발생 시 스택 기록 |
| dom_inspect_*.txt | 〃 | 화면 진단 리포트 (진단 도구 사용 시) |

---

## 7. 빌드·배포

```
dotnet build                 # 전체 빌드
dotnet test                  # 55개 테스트
dotnet run --project src/NeisAutoFill.App
```

**릴리스** (버전 3곳 = csproj Version / git 태그 / 릴리스 제목)
```
1. src/NeisAutoFill.App/NeisAutoFill.App.csproj 의 <Version> 올리기
2. dotnet publish src/NeisAutoFill.App -c Release -r win-x64 --self-contained true -o publish/NeisAutoFill
3. Compress-Archive publish/NeisAutoFill publish/NeisAutoFill-win-x64.zip -Force
4. gh release create vX.Y.Z publish/NeisAutoFill-win-x64.zip --title "..." --notes-file notes.txt
→ 기존 사용자는 다음 실행 때 UpdateService가 감지해 업데이트 안내
```

**공개 저장소 제외** (.gitignore): code.gs, Index.html, 주소.txt, 개발노트,
assets/ref(학생 이름 포함 캡처), assets/mockups, *.xlsx — 개인정보·GAS 주소 보호

---

## 8. 남은 과제 / 미검증

- **서술문 나이스 입력 실기기 검증** — 셀렉터는 실측 완료, 실제 다건 입력은 소규모만 확인
- **하이브리드 순서 매칭 실기기 검증** — 로직·단위테스트 완료, 실화면 확인 권장
- **관련 문서**: `DESIGN_설계도.md`(초기 설계), `ROADMAP_개발로드맵.md`(초기 로드맵),
  `docs/보관_진단_검증도구.md`(진단·dry-run 복원법), `NEIS_자동입력기_개발노트.md`(프로토타입 인수인계, 로컬 전용)
```
