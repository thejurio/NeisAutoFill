# NeisAutoFill 개선 로드맵 (v2.x)

> 2026-07-17 사용자 요청 기반 대규모 개선 계획.
> **이 문서는 진행 상황 추적용** — 각 항목의 상태를 작업할 때마다 갱신한다.
> 초기 개발 기록은 [개발문서.md](개발문서.md), 최초 로드맵은 [ROADMAP_개발로드맵.md](ROADMAP_개발로드맵.md) 참고.

상태 표기: ⬜ 대기 · 🔄 진행중 · ✅ 완료 · ⏸ 보류

---

## 1. 사용자 요청 (2026-07-17)

| # | 요청 | 반영 Phase |
|---|---|---|
| 1 | 성취기준 양식(이지에듀/스쿨마스터)을 PDF·HWP로 넣으면 AI가 프로그램용 데이터로 변환 | Phase 3 |
| 2 | 학생 명단을 엑셀 없이 프로그램 내 입력 (복사붙여넣기 일괄 입력) | Phase 2 |
| 3 | 성취기준·학생 명단을 프로그램 내에서 수정 | Phase 2 |
| 4 | 성적표 다중 셀 복사/붙여넣기 + 선택 셀 일괄 등급 지정 | Phase 2 |
| 5 | 성적 편집 화면에 현재 과목 성취기준 토글 패널 | Phase 2 |
| 6 | 서술문 전과목/선택과목 일괄 생성 | Phase 4 |
| 7 | 모든 자료를 엑셀 파일로 저장 — 사용자가 엑셀로 직접 수정 가능 | Phase 1 |
| 8 | 시작 시 최근 자료 자동 로드 + 원하는 자료 선택 로드 | Phase 1 |
| 9 | 모든 자료 입력을 불러오기·드래그앤드롭으로 | Phase 1·3 |
| 10 | AI 서술문 생성을 백그라운드로 (프로그램 안 멈춤) | Phase 4 |
| 11 | 나이스 입력 검증 완화 — 이름/과목/영역 불일치 시 사용자 결정 (매핑·순서지정) | Phase 5 |
| 12 | GAS 로그를 생성요청당 1건 + 실행 기록 + 키 로테이션 + 키 상태(마지막사용·한도초과) 기록 | Phase 6 |
| 13 | 그 외 UI/UX 개선 제안 | Phase 7 |
| 14 | (추가) 등급·서술문 **전과목 자동 업로드** — 프로그램이 나이스 화면·과목을 알아서 이동하며 전부 입력 | Phase 5.5 |

## 2. 확정된 결정사항

- **전과목 업로드 저장 정책 (A안)**: 전과목 모드에서만 "검증 통과 시 과목별 자동 [저장]" 허용.
  단일 과목 입력은 기존대로 저장 안 함. 시작 전 명시 동의, 검증 실패 과목은 저장 없이 중단·리포트.
- **HWP 처리 파이프라인**: pdf→Gemini 직접 / hwpx→XML 텍스트 추출 / hwp→한컴 COM으로 PDF 변환(설치 시, 보안모듈 자동등록), 미설치 시 바이너리 텍스트 추출 폴백.
- **AI 양식 인식은 범용 프롬프트 1개** — 업체별 파서를 만들지 않는다. 샘플 6종(ref/, 이지에듀·스쿨마스터 × pdf·hwp·hwpx)은 테스트 케이스.
- **키 로테이션은 DooClick 키서버 로직 이식** — APIKeys 시트(A:키, B:상태, C:마지막사용) 공유.
  마지막사용 오래된 순 선택 + 사용 시 C열 갱신 + 429 시 "한도초과" 기록 후 다음 키 재시도.
- **AI 가져오기 결과는 검토 화면에서 확인 후 적용** (바로 반영 금지 — 오인식 방지).

## 3. Phase별 계획·진행 상황

### Phase 0 — 준비 ✅
- ✅ ref/ 정리 커밋 (기존 DooClick 참고 코드 삭제 반영 + .gitignore에 ref/, *.zip 추가 — 로컬 샘플은 untracked로 보존됨)
- ✅ 샘플 자료 확보 (ref/ 6종) · code.txt(DooClick 키서버) 확인 · APIKeys 시트 컬럼 확정

### Phase 1 — 작업공간 기반 (요청 7·8·9) ✅
모든 인앱 편집 기능의 저장 토대. 가장 먼저.
- ✅ 작업공간 폴더 `문서\NeisAutoFill\` — 양식·성적·서술문 저장 대화상자 기본 위치 (`AppPaths.Workspace`)
- ✅ 성적 자동 저장 — 편집 2초 후 디바운스 저장(`MainViewModel.AutoSaveGrades`), 파일 잠금 시 로그만 남기고 dirty 유지, 종료 시 조용히 재시도 후 실패 때만 질문
- ✅ 시작 시 최근 자료 자동 로드 — `AppStateStore`(state.json)에 마지막 평가계획서·성적파일 기록, 시작 시 복원 (실패는 로그만)
- ✅ 툴바 [🕘 최근 ▾] 메뉴 — 최근 10개(계획서/성적 구분), 실존 파일만 표시. 기존 열기·드래그앤드롭 유지
- 서술문 xlsx 자동 미러·재불러오기는 Phase 4에서 (생성기 개편과 함께)
- 평가계획 인앱 편집분 자동 저장은 Phase 2에서 (편집 기능과 함께)

### Phase 2 — 프로그램 내 편집 (요청 2·3·4·5) ✅
- ✅ [📝 명단·계획] 편집 창(`PlanEditorWindow`) — 학생 명단: 엑셀 복사 → 한 번에 붙여넣기(전체 교체, 번호/이름 자동 인식·헤더 스킵·이름만 복사 시 순번 자동), 행 추가/삭제
- ✅ 평가계획 편집 그리드 — 행=영역, 열=영역|성취기준|등급별 기준내용. 과목 추가/삭제/이름 변경, 엑셀 표 Ctrl+V. 저장 시 `PlanWorkbookWriter`로 작업공간 `평가계획서.xlsx`에 기록 후 재로드 (엑셀 직접 수정과 동일 경로 — 요청 7 충족)
- ✅ 성적표 다중 셀 선택(`SelectionUnit=CellOrRowHeader`) + Ctrl+C/Ctrl+V — 등급 셀은 척도 화이트리스트 검증, 허용외 값은 건너뛰고 로그
- ✅ 선택 셀 일괄 등급 지정 — 우클릭 메뉴(척도 변경 대응 동적 구성) + 숫자 1~9 단축키 + Del 지우기
- ✅ 성취기준 참조 패널 — 각 과목 탭 [📖 성취기준] 토글, 우측 300px 패널에 영역·성취기준·등급별 기준 표시, 토글 상태 저장(state.json)
- 신규 공용 코드: `ClipboardTable`(Core, TSV 파싱·명단 해석), `PlanWorkbookWriter`(Excel, 로더와 round-trip 보장), `DataGridClipboard`(App, 표 붙여넣기) — 테스트 8개 추가

### Phase 3 — AI 양식 가져오기 (요청 1·9) ⬜
- ⬜ 파일 판별·추출 파이프라인 (pdf/hwpx/hwp — 위 결정사항 참조)
- ⬜ 한컴 COM 변환기 + 보안모듈 1회 자동 등록
- ⬜ GAS `parsePlan` 액션 (파일/텍스트 → 구조화 JSON, 스키마: 과목/영역/성취기준/등급별 기준)
- ⬜ 가져오기 검토 창 (AI 결과 편집 후 [적용])
- ⬜ 샘플 6종 전부 인식 검증
- ⬜ 드래그앤드롭에 pdf/hwpx/hwp 연결

### Phase 4 — 생성기 개선 (요청 6·10) ✅
- ✅ 백그라운드 생성 큐 (`GenerationQueue`) — 동시 2건, 창 닫기·과목 전환에도 계속, 완료 즉시 narratives.json 저장, [⛔ 생성 중지], 진행바+상태(“생성 12/48 — 창을 닫아도 계속…”)
- ✅ 전과목/선택과목 일괄 생성 — [🌐 전과목 생성] → 과목 체크리스트 대화상자(생성 가능 인원 표시) → 선택 과목 전체 큐 투입
- ✅ 서술문 xlsx 저장·불러오기 — [💾 저장]=작업공간 `서술문.xlsx` (사용자 피드백으로 자동 미러 → 수동 저장 전환), [📤 내보내기]=위치 선택, [📂 불러오기]=엑셀 수정분 되가져오기 (`NarrativeWorkbookLoader`, round-trip 테스트 2개). 툴바 기능별 2줄 재배치 (1줄 생성 / 2줄 자료·입력)

### Phase 5 — 나이스 입력 대화형 매핑 (요청 11) ✅ (실기기 검증 남음)
- ✅ 입력 전 "매칭 미리보기 창"(`MatchPreviewWindow`) — 엔진이 화면(행지도)을 읽은 뒤 `resolveMatch` 콜백으로 UI 가 검토·결정. 문제 없으면 창 생략하고 바로 실행
- ✅ 과목명 불일치: 경고 배너 + "이대로 입력 동의" 체크 필수
- ✅ 학생 이름 불일치: 유사도 자동 제안(포함/편집거리 1) + 드롭다운으로 엑셀 학생 확정 or 입력 안 함
- ✅ 영역명 불일치: 영역 매핑 드롭다운 (자동 추천 포함)
- ✅ 영역 수 불일치/중복: 순서 매핑 — 화면 행 순서별로 입력할 엑셀 영역 선택('입력 안 함'=건너뜀), 같은 영역 중복 매핑 방지 검증. 화면 영역명 중복 시 순서 모드 강제
- ✅ `StudentMatcher` areaMap/nameMap 오버라이드 + ByOrder 빈 위치 건너뜀, `MatchAnalyzer` 신설 — 테스트 6개 추가 (71/71)
- ⬜ **실기기 검증** — 실제 나이스 화면에서 불일치 시나리오 확인 (Phase 5.5 실측과 함께)

### Phase 5.5 — 전과목 자동 업로드 (요청 14) ⬜
- ⬜ 나이스 메뉴·과목 콤보·[조회] 자동 조작 (셀렉터 실측 필요 — DomInspector 덤프)
- ⬜ 전과목 오케스트레이터: 과목 선택→조회→입력→전체 재검증→**통과 시 [저장]**→다음 과목 (A안)
- ⬜ 등급([교과별 평가])·서술문([학기말 종합의견]) 두 화면 모두 지원
- ⬜ 시작 전 전과목 매칭 미리보기(Phase 5 재사용) + 자동 저장 동의
- ⬜ 중간 실패 리포트 ("여기까지 저장됨, X과목부터 실패")
- ⬜ 개발문서.md 안전정책 문구 개정

### Phase 6 — GAS 로깅·API 키 관리 (요청 12) ⬜
code.gs 수정 + 재배포 필요. 다른 Phase와 독립 — 언제든 병행 가능.
- ⬜ 키 로테이션: 마지막사용 오래된 "사용가능" 키 선택 + C열 갱신
- ⬜ 429/한도초과 시 B열 "한도초과" 기록 + 다음 키 자동 재시도
- ⬜ 로그를 학생당 → 생성요청(배치)당 1건 (과목·인원수 기록)
- ⬜ startup 기록 동작 확인 (코드는 v1.2.2에 있음 — GAS 배포본이 구버전인지 확인)
- ⬜ GAS 재배포 + 프로그램 URL 갱신 확인

### Phase 7 — UI/UX 다듬기 (요청 13) ⬜
- ⬜ 성적 편집 Undo (Ctrl+Z) — 일괄 지정·붙여넣기 실수 복구
- ⬜ 작업 상태바 — 백그라운드 생성·입력 진행을 메인 하단에 상시 표시
- ⬜ 첫 실행 온보딩 (① 평가계획 → ② 명단 → ③ 성적 단계 안내)
- ⬜ 성적표 미입력 셀 강조
- ⬜ 설정 창 통합 (지역/척도/바이트제한 등)

## 4. 진행 순서

**0 → 1 → 2 → 4 → 5 → 5.5 → 3 → 6 → 7**

- 1·2가 먼저: 3(AI 가져오기) 결과물이 저장·편집될 곳이 필요
- 3은 외부 의존(샘플 검증·GAS 재배포·프롬프트 튜닝)이 커서 기반 완성 후
- 6은 독립적 — code.gs 준비된 상태이므로 틈나는 대로 병행
- 5.5는 5의 매핑 인프라 + 실기기 셀렉터 실측을 재사용

## 5. 진행 기록

| 날짜 | 내용 |
|---|---|
| 2026-07-17 | 요청 14건 분석, Phase 0~7 계획 수립. A안(전과목 모드 과목별 자동저장) 확정. HWP 파이프라인·범용 프롬프트 방침 확정. 샘플 6종·DooClick 키서버 코드 확보, APIKeys 시트 구조 확인. 본 문서 작성. |
| 2026-07-17 | **Phase 0·1 완료.** ref/ 정리 커밋. AppStateStore(최근·마지막 파일)·시작 시 자동 로드·[최근 ▾] 메뉴·성적 디바운스 자동 저장·작업공간 폴더 구현. 빌드 클린(경고 0)·테스트 55/55 통과·앱 부팅 확인. |
| 2026-07-17 | **Phase 2 완료.** 명단·평가계획 인앱 편집 창(붙여넣기 일괄 입력), 성적표 다중 셀 복붙·일괄 등급 지정(우클릭/1~9/Del), 성취기준 토글 패널. 테스트 63/63 통과·부팅 확인. ※ 실사용 UI 확인 권장 (편집 창·패널은 자동테스트 밖) |
| 2026-07-17 | **Phase 5 완료 (코드)** (`20171d2`). 매칭 미리보기 창 — 과목 배너/학생·영역 매핑(자동 추천)/순서 매핑(부분 선택). 엔진 콜백 구조로 전환(기존 즉시 차단 제거), StudentMatcher 오버라이드. 테스트 71/71. 실기기 검증은 5.5와 함께. 생성기 툴바 2줄 재배치 + 서술문 수동 [저장] 전환 (사용자 확인 "이뻐졌네") |
| 2026-07-17 | **Phase 4 완료.** 백그라운드 생성 큐(동시 2건·중지·진행바), 전과목 일괄 생성(과목 선택 대화상자), 서술문.xlsx 자동 미러+엑셀 불러오기. 테스트 65/65·부팅 확인. ※ 실제 GAS 생성은 실사용 확인 필요 |
| 2026-07-17 | **Phase 2 실사용 피드백 반영** (`d07e318`). ① 명단·계획 ↔ 성적표 동기화 (학생 추가/삭제가 전 과목 탭 반영, 성적은 번호·이름 이월) ② 계획+명단만으로 성적표 자동 생성 → 작업공간 성적.xlsx (엑셀 없이 처음부터 진행 가능) ③ 명단 자동 번호(최대+1)·행 삭제 시 번호 당김 ④ 붙여넣기 기준점 = 선택 좌상단, 단일 값은 선택 전체 채움 ⑤ 일괄 입력 버튼 바·전체 선택·이름/영역 헤더 클릭 선택 ⑥ 스플릿 버튼([성적파일][▼])·빈 셀 시드. AI 가져오기 버튼 자리는 [📄 평가계획 불러오기]로 명명 (Phase 3에서 활성화) |
